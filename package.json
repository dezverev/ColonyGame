{
  "name": "colonygame",
  "version": "0.1.0",
  "description": "Isometric multiplayer space colony 4X game built with Three.js",
  "scripts": {
    "dev": "concurrently \"node src/dev-client-server.js\" \"node --watch server/server.js\"",
    "start": "node server/server.js",
    "test": "node --test src/tests/**/*.test.js",
    "stop": "lsof -ti :4000,:4001 | xargs kill -9 2>/dev/null || true"
  },
  "dependencies": {
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
