# ColonyGame — Design & Implementation Roadmap

Isometric multiplayer space colony 4X game. Players found colonies on alien worlds, research technology, build fleets, explore the galaxy, and compete or cooperate. Rendered with Three.js in isometric 3D. Inspired by Stellaris.

## Architecture

- **Static file server** (port 4000): `src/dev-client-server.js` — serves client files
- **WebSocket game server** (port 4001): `server/server.js` — room management, game state, tick loop
- **Client**: Vanilla JS + Three.js — isometric colony view (primary), 3D galaxy map, system view
- **Rendering**: Three.js OrthographicCamera for isometric, PerspectiveCamera for galaxy/system views
- **Performance escape hatch**: If server-side computation becomes a bottleneck (galaxy generation, large fleet combat resolution, pathfinding), extract hot paths into Rust compiled to WASM and call from Node.js. Keeps the Node.js server for networking/game loop while offloading heavy math to near-native speed. No need until profiling shows a real bottleneck

## Phases

### Phase 1: Foundation Pivot
- [x] **BALANCE FIX — Energy economy:** Rebalance DISTRICT_DEFS energy consumption — Industrial consumes 3 energy/month (not 50), Research consumes 4 energy/month (not 100). Increase Generator output from 4 to 6 energy. Reduce Industrial build cost to 200 minerals (remove energy cost or reduce to 10). Reduce Research build cost to 200 minerals + 20 energy. Add Housing energy consumption of 1/month. Update all tests to match new values
- [x] **BALANCE FIX — Starting food deficit + housing:** Change starting pre-built districts from (1 Generator, 1 Mining, 1 Agriculture) to (1 Generator, 1 Mining, 2 Agriculture) so food production (12) exceeds consumption (10 pops × 1 = 10). Also increase base capital housing from 2 to 10 so starting 10 pops don't exceed housing on turn 1. Update _initStartingColonies and related tests
- [x] **BALANCE FIX — Pop growth pacing:** Add `colony.growthProgress` counter incrementing each tick when food surplus > 0. Base growth: +1 pop every 400 ticks (40 seconds). Accelerated growth: food surplus > 5 → 300 ticks, surplus > 10 → 200 ticks. Enforce housing cap on growth (pops cannot exceed housing). Add pop growth tests. This is the core engine of empire expansion
- [x] **BALANCE FIX — Early mineral pacing:** Increase Mining district output from 4 to 6 minerals/month to reduce the early-game mineral wall (currently takes 37 seconds per mineral build funded by mining income alone). Also increase starting minerals from 200 to 300 to give 2 immediate builds. Adjust Mining build cost to 100 minerals (same as Agriculture/Housing). Update related tests
- [x] Refactor game-engine.js from RTS to colony 4X: remove unit movement/combat, add colony state (districts[], buildings[], pops, resources), planet properties (size, type, habitability). Keep tick loop and room integration
- [x] New resource system in game-engine.js: track per-player resources {energy, minerals, food, alloys, research: {physics, society, engineering}, influence}. Calculate production/consumption per "month" (every 100 ticks = 10 seconds), not per tick. Starting resources: 100 energy, 200 minerals, 100 food, 50 alloys, 100 influence. Start with 10 pops and 3 pre-built districts (1 Generator, 1 Mining, 1 Agriculture) so resources flow immediately
- [x] Update network protocol: replace moveUnits/gameCommand with colony commands (buildDistrict, buildBuilding, demolish). Server validates ownership, resources, available slots. Client sends command, server processes on next tick
- [x] Update all tests: remove RTS unit/combat tests, add colony creation, resource calculation, district building, protocol validation tests. Minimum 15 tests covering the new systems
- [x] **BALANCE FIX — Starting pop/housing deadlock:** Reduce starting pops from 10 to 8 in `_createColony`. Players start 2 below housing cap (10), allowing 2 natural growth cycles (~40 sec each at surplus +4) before housing constrains. Teaches players that pops grow and Housing matters. Update food consumption in starting balance (8 pops × 1 = 8 food, surplus becomes +4 instead of +2). Update all tests referencing starting pops=10
- [x] **BALANCE FIX — Generator cost parity:** Reduce Generator district cost from 150 to 100 minerals in DISTRICT_DEFS. All 4 basic districts (Housing, Generator, Mining, Agriculture) now cost 100 minerals uniformly. Industrial and Research remain at 200 (clearly "tier 2"). Update tests referencing generator cost
- [x] **BALANCE FIX — Variable build times:** Change build times by district tier: Housing 200 ticks (20 sec), Generator/Mining/Agriculture 300 ticks (30 sec), Industrial/Research 400 ticks (40 sec). Update DISTRICT_DEFS buildTime values. Quick Housing lets players unblock growth fast; slower advanced districts create anticipation. Update build time tests
- [x] **Colony idle event notifications:** Add server-side `gameEvent` broadcasting in game-engine.js. Events: `constructionComplete` (when buildQueue item finishes), `queueEmpty` (when last item completes), `popMilestone` (every 5 pops), `housingFull` (when pops reach housing cap), `foodDeficit` (when food goes negative). New method `_emitEvent(type, playerId, details)` stores events in `this._pendingEvents[]`, flushed with gameState broadcast. Rate-limit popMilestone to fire only on multiples of 5
- [x] **Energy deficit consequences:** When a player's energy is negative at monthly processing, disable the highest-energy-consuming district across their colonies until energy balance is non-negative. Disabled districts: `district.disabled = true`, produce nothing, consume nothing, pop becomes unemployed. Re-check each month and re-enable (cheapest first) when energy supports them. Send `districtDisabled`/`districtEnabled` events. Modify `_calcProduction` to skip disabled districts. Add tests for disable/re-enable behavior
- [x] **CLIENT UX SPRINT 1/3 — Single-player/practice mode:** Allow solo game launch without requiring 2 players. Add a "Practice" option to room creation that sets maxPlayers to 1 and bypasses the ready check in `canLaunch`. Solo rooms auto-launch when the host clicks "Launch Game". Enables rapid iteration and solo playtesting. In room-manager.js, modify `canLaunch` to return true when `room.maxPlayers === 1` or `room.practiceMode === true`, skipping the `players.size < 2` check
- [x] **CLIENT UX SPRINT 2/5 — Stale client cleanup:** Strip all RTS rendering code from app.js — remove Canvas 2D game renderer, Projection module references, unit selection, minimap, gold/wood/stone HUD. Update gameInit handler to parse colony 4X state (colonies, players, yourPlayerId). Update gameState handler to receive resource/colony updates. Remove minimap canvas, gold/wood/stone resource bar HTML, selection panel HTML from index.html. Add `<div id="colony-ui">` container for HTML overlay and `<div id="render-container">` for Three.js canvas. Remove projection.js script tag. Keep: WebSocket connection, screen management, chat, room UI, handleMessage dispatch structure
- [x] **CLIENT UX SPRINT 3/5 — Three.js scene + isometric colony view:** Add Three.js via CDN in index.html. Create renderer.js module: initialize Scene, OrthographicCamera (isometric angle: 35.264° pitch, 45° yaw), WebGLRenderer sized to render-container. Add ambient + directional light. Run requestAnimationFrame loop. Generate colony terrain as a grid of BoxGeometry tiles (planet.size slots arranged in 4-column rows), each tile ~1 unit wide with 0.1 gap. Ground plane underneath. Camera positioned to frame the full grid. Isometric camera controls: scroll-wheel zoom (adjust ortho frustum), middle-mouse drag to pan, WASD/arrow keys to pan. Clamp zoom to min/max bounds. Camera always maintains isometric angle. Dark space skybox or background color (#0a0a1a)
- [x] **CLIENT UX SPRINT 4/5 — 3D district rendering:** Render built districts as colored 3D boxes sitting on grid tiles — Generator=yellow (#f1c40f) tall prism, Mining=gray (#95a5a6) low wide box, Agriculture=green (#2ecc71) dome-ish shape, Industrial=blue (#3498db) angular factory, Research=purple (#9b59b6) spire, Housing=white (#ecf0f1) block. Empty slots: dark semi-transparent tile with subtle glow. Under-construction: wireframe of target shape with progress fill (partially opaque). Use MeshStandardMaterial for all. Clickable: Raycaster on mouse click detects which tile was hit — click empty tile to select for building, click built district for info/demolish. Update 3D scene from gameState on each refresh (add/remove/update meshes to match colony.districts). Highlight selected tile with emissive outline
- [x] **CLIENT UX SPRINT 5/5 — HTML overlay UI on 3D view:** Build HTML/CSS overlay positioned over the Three.js canvas. Resource bar (top): all 6 resource types showing stockpile and net income/month colored green(+)/red(−). Colors: energy=#f1c40f, minerals=#95a5a6, food=#2ecc71, alloys=#e67e22, research=#3498db, influence=#9b59b6. Status bar: month counter (tick/100), pop count with housing cap warning, growth indicator (slow/fast/rapid/starving/housing full) with progress bar. Colony info panel (right side, semi-transparent bg): colony name, planet type/size, district count, pop breakdown (working/idle), housing used/cap, build queue with progress bars (ticks as seconds) and cancel buttons. Build menu (bottom, appears on empty tile click): district type buttons with cost, grayed if unaffordable or queue full (3 max), hover shows production preview (+X resource/month). Wire: build click sends `buildDistrict`, demolish sends `demolish`, queue cancel sends `demolish` with queue item ID. CSS dark space theme: panels rgba(26,26,46,0.85) with backdrop-blur, borders #2a2a4e. Monospace for numbers. Throttle UI data refresh to 2Hz, Three.js renders at 60fps independently
- [x] **Build menu resource header:** Show current mineral and energy stockpile at the top of the build menu panel so players can see affordability without the full resource bar. Format: "Minerals: 300 | Energy: 100" in monospace. Updates on each gameState refresh. Lightweight UX fix that makes the build menu self-contained before Sprint 5/5 resource bar exists
- [x] **BALANCE FIX — Dead code:** The first-3-districts 50% build time discount never fires because starting colonies already have 3 pre-built districts. Change to: first 3 *player-built* districts on any newly colonized planet (not the starting planet) build at 50% time. Track colony.isStartingColony flag
- [x] Planet type signature bonuses: each planet type gets a distinct mechanical bonus — Continental (+1 all district output), Ocean (+50% Agriculture, +2 max districts), Tropical (+25% Agriculture, +25% Research), Arctic (+50% Research), Desert (+50% Mining, +25% Energy), Arid (+25% Mining, +25% Alloys). Apply bonuses to district output calculations. Show bonuses in colony view and system view
- [x] **Game speed controls:** Implement setGameSpeed (1-5) and togglePause in server.js. Speed multiplier affects MONTH_TICKS: Speed 1 = 200 ticks/month, Speed 3 = 100 (default), Speed 5 = 50 ticks/month. Construction times scale with speed. Add speed selector to room settings. Show speed/pause state in HUD. Host-only control during gameplay. Keyboard shortcuts: +/- for speed, Space for pause. Essential for playtesting and experienced-player flow
- [x] **In-game chat:** Extend existing lobby chat to work during gameplay — route chat messages to all players in the same room while game is running. Show chat panel as collapsible overlay on game screen. Low effort, high social value for multiplayer awareness
- [x] **Scoreboard overlay (Tab key):** Press Tab to show/hide a player scoreboard during gameplay. Display: player name, colony count, total pops, resource income rates (energy/minerals/food/alloys), month counter. All info is public — in short browser matches, outplaying with shared info is more fun than information asymmetry
- [x] **Event ticker for player actions:** Add a scrolling text ticker at the top of the game screen that narrates significant player actions: "Player X built a Research district", "Player Y's colony reached 20 pops", "Player Z built their 5th Mining district". Server broadcasts `gameEvent` messages on construction complete, pop milestones (every 5 pops), and colony trait earned. Creates multiplayer awareness without requiring galaxy view
- [ ] **BALANCE TWEAK — Colony ship cost and build time reduction (game-designer R41):** Reduce colony ship cost from `{ minerals: 200, food: 100, alloys: 100 }` to `{ minerals: 150, food: 50, alloys: 75 }` in COLONY_SHIP_COST. Reduce COLONY_SHIP_BUILD_TIME from 600 to 400 ticks (40 seconds). Current cost+time puts second colony arrival at ~17.5 minutes in a 20-minute match — too late for meaningful development. New values target ~12 minutes for second colony, leaving 8 minutes to develop it. Update COLONY_SHIP_COST and COLONY_SHIP_BUILD_TIME constants in game-engine.js. Update client build menu to show new costs. Update all colony ship cost/build time tests
- [x] **Live scoreboard with opponent summaries (game-designer R41-1):** Include other players' VP, colony count, pop count, tech count, and raiders destroyed in the per-player gameState broadcast. Currently each player only sees their own data. Add `opponents` array to `getPlayerState()` containing `{ id, name, color, vp, colonies, pops, techs, raidersDestroyed }` for all other players (computed from `_calcVPBreakdown` and `_playerColonies`). Client Tab scoreboard already renders a table — update it to show all players ranked by VP with breakdown columns. Highlight current player's row. Update at broadcast rate (~3.3Hz). No new VP data needed — just expose existing server-side computations to the client. Add opponent summary serialization tests
- [ ] **BALANCE TWEAK — Defense platform repair rate increase (game-designer R41):** Increase DEFENSE_PLATFORM_REPAIR_RATE from 10 to 15 HP/month in game-engine.js. At 15 HP/month, a platform fully repairs from 10 HP (post-raider combat) to 50 HP in ~2.7 months instead of 4 months. Makes sequential raider attacks slightly less devastating while still requiring strategic defense timing. Update defense platform repair tests
- [ ] **Starting planet variety:** On game start, randomly assign each player's starting planet type from habitable types (Continental, Ocean, Tropical, Arctic, Desert, Arid) with size randomized in range 12-20. In multiplayer fairness mode (default), all players get the same random type and size. Requires planet type bonuses to be implemented first. Show planet type and size in colony panel. Modify `_initStartingColonies` to use random selection instead of hardcoded Continental size 16
- [x] **Score timer victory condition:** Add configurable match timer (selectable in room settings: 10/20/30 min or unlimited). Timer starts at game launch, counts down in server ticks. VP formula: pops×2 + districts×1 + alloys_stockpiled/50 + total_research/100. When timer expires, broadcast `gameOver` with winner (highest VP), per-player scores, and breakdown. Show live VP on Tab scoreboard. Add 2-minute warning and 30-second "final countdown" events. In final 30 seconds, all player scores become visible to everyone. Default: 10 minutes for practice, 20 minutes for multiplayer. Add `setMatchTimer` to room settings, `gameOver` handler in server.js. Store timer as `this._matchTicksRemaining` in game-engine, decrement each tick
- [ ] **Scarcity pre-warning system:** When a scarcity season is about to start, broadcast a warning event 1 month (100 ticks) in advance: "WARNING: Mineral scarcity detected — mining output will drop 25% next month." Track `this._pendingScarcity = { resource, warningMonth }` in game-engine. Warning gives players time to react (stockpile, shift production) rather than being blindsided, turning scarcity from frustration into strategic opportunity
- [ ] **Colony saturation indicator:** When a colony has filled 80%+ of district slots, show a "Nearing Capacity" warning in colony panel. When 100% full, show "Fully Developed" badge. In multiplayer event ticker, announce when any player's colony reaches full development. Creates a milestone moment and signals to the player that they should expand to new colonies. Track `colony.districts.length / colony.planet.size` ratio in colony serialization
- [ ] **BALANCE TWEAK — Starting minerals & dead resources:** Reduce starting minerals from 300 to 250 (forces opening choice: 2 basic + 50 buffer, or save for 1 advanced district). Set starting alloys from 50 to 0 (no sink exists yet — remove confusion). Keep starting energy 100, food 100, influence 100. Update `_initPlayerStates` and all tests referencing starting resource values
- [x] **BALANCE TWEAK — Research & Industrial output bump:** Increase Research district output from 3/3/3 to 4/4/4 per type. Increase Industrial district output from 3 to 4 alloys. At 200 minerals + energy cost, tier-2 districts should produce meaningfully more than basic districts to justify their premium. Update DISTRICT_DEFS and all affected tests
- [x] **Scoreboard overlay (Tab key) — server VP calculation:** Add `_calcVictoryPoints(playerId)` to game-engine: VP = pops×2 + districts×1 + alloys_stockpiled/50 + total_research/100. Include VP in `getState()` per-player data. Scoreboard UI rendering is handled by Client UX Sprint 5. This task is server-side VP logic only. Add VP calculation tests
- [ ] **PRIORITY: Surface anomalies on colony grid (moved from Phase 7) — server logic:** When creating a colony, randomly place 1-3 tile anomalies on the district grid. Types: "Mineral Vein" (+50% output to mining district built on this tile), "Thermal Vent" (+50% output to generator on this tile), "Fertile Soil" (+50% output to agriculture on this tile), "Ancient Ruins" (excavate for +500 research or preserve for +2 influence/month — one-time choice), "Alien Artifact" (+200 alloys or +300 research — one-time choice). Track as `colony.anomalies = [{ tileIndex, type, resolved }]`. Anomalies make district placement a spatial puzzle rather than just type selection. Add `resolveAnomaly` command. Render anomaly markers on colony grid tiles in Three.js (Sprint 4/5). This brings exploration-like discovery to the colony view without requiring the galaxy. Implement server-side logic first (anomaly generation, tile bonuses in _calcProduction, command handler), then visual rendering with Sprint 4/5. Add anomaly tests
- [ ] **Surface anomaly 3D rendering:** Render anomaly markers on colony grid tiles in Three.js. Each anomaly type gets a distinct glowing crystal/rune embedded in the ground tile: Mineral Vein=silver glow, Thermal Vent=orange glow, Fertile Soil=green glow, Ancient Ruins=cyan glow, Alien Artifact=purple glow. Use MeshStandardMaterial with emissive color and pulsing emissiveIntensity (sin wave, 0.3-0.8). When a district is built on an anomaly tile, show the bonus in the district info panel (+50% output indicator). Anomaly markers visible through empty tiles but hidden under built districts (bonus still applies). Requires surface anomalies server logic to be implemented first
- [x] **BALANCE: Mini tech tree research costs adjustment:** When implementing the Mini Tech Tree (Phase 2), use adjusted costs for 20-minute match pacing: Physics T1 (Improved Power Plants, +25% Generator) = 150 research, Society T1 (Frontier Medicine, +25% pop growth) = 150, Engineering T1 (Improved Mining, +25% Mining) = 150. Physics T2 (Advanced Reactors, +50% Generator) = 500, Society T2 (Gene Crops, +50% Agriculture) = 500, Engineering T2 (Deep Mining, +50% Mining) = 500. At 3 research/month from 1 Research district, T1 completes in 50 months (~8 min), T2 in ~167 months. With 2 Research districts (6/month), T1 = 25 months (~4 min), T2 = ~83 months (~14 min). This pacing rewards early research investment without making tech trivial. Update the Mini Tech Tree task costs in Phase 2 to match these values
- [ ] **Disabled district 3D rendering:** When energy deficit auto-disables a district, render it with desaturated material (MeshStandardMaterial with color #444444, opacity 0.5, emissive off). Add a small red "X" or red tint overlay to distinguish disabled from active districts. On re-enable, restore original district color. Requires energy deficit consequences to be implemented first
- [x] **PRIORITY: Score timer + VP scoring (game-designer R13):** Implement score timer victory and VP calculation together as a single deliverable. Server: `_calcVictoryPoints(playerId)` (VP = pops×2 + districts×1 + alloys_stockpiled/50 + total_research/100), `_matchTicksRemaining` countdown, `gameOver` broadcast. Client: Tab scoreboard showing live VP, 2-minute warning event, 30-second final countdown with all scores visible, post-game overlay with winner and breakdown. Room settings: timer select (10/20/30 min or unlimited). Default 10 min practice, 20 min multiplayer. This is the single highest-impact feature — it transforms the game from sandbox to competitive. Combines the existing "Score timer victory condition" and "Scoreboard overlay — server VP calculation" tasks
- [x] ~~**PRIORITY ORDER (game-designer R14):** Superseded by R15 course correction — galaxy and ships are now the priority, not colony polish~~
- [x] ~~**PRIORITY ORDER (game-designer R15 — COURSE CORRECTION):** Superseded by R17 priority order. Galaxy generation is done~~
- [x] **PRIORITY ORDER (game-designer R17):** Galaxy generation is complete. The critical path is now visualization and interaction. `/develop` should implement in this order: **(1) Galaxy map Three.js view (Phase 3 R17-1)**, **(2) Galaxy/colony view toggle with G key (Phase 1)**, **(3) System selection panel (Phase 3 R17-7)**, **(4) Alloy VP fix + industrial output bump (Phase 1 R17-2)**, **(5) Event toast HUD (Phase 1 R17-6)**, **(6) Building system (Phase 2)**, **(7) Tech tree T3 expansion (Phase 4 R17-5)**, **(8) Fog of war (Phase 3 R17-4)**, then resume Phase 3: science ships, colony ships, fleet movement
- [x] **Galaxy/Colony view toggle (G key):** Add keyboard shortcut 'G' to toggle between galaxy map view and colony view. In app.js, track `currentView = 'colony' | 'galaxy'`. When switching, show/hide the appropriate Three.js scene and UI panels. Colony view shows isometric grid (existing). Galaxy view shows galaxy map (requires galaxy map view to be implemented first). Store last-selected system and colony so switching back restores context
- [x] **Colony list sidebar:** When player has 2+ colonies, show a sidebar list on the left of the colony view. Each entry: colony name, pops, net income summary. Click to switch which colony the isometric view renders. Number keys 1-5 as shortcuts. Highlight current colony. Required for multi-colony to be usable
- [x] **Max colonies cap (5 per player):** Enforce a maximum of 5 colonies per player in the colonize command handler. Prevents runaway expansion in 20-minute matches. Show "Colony cap reached" error if player tries to found a 6th colony. Can be increased by techs later
- [x] **BALANCE FIX — Alloy VP weight + Industrial output bump (game-designer R17-2, R18-2):** Combined delivery. (1) Change VP formula from `alloys/50` to `alloys/25` in `_calcVictoryPoints` and `_triggerGameOver` breakdown. (2) Increase Industrial alloy output from 3 to 4 in DISTRICT_DEFS. (3) Increase Research output from 3/3/3 to 4/4/4 in DISTRICT_DEFS. (4) Update client DISTRICT_UI to show "+4 Alloys" and "+4 Phys/Soc/Eng". (5) Update all affected tests. At 4 alloys/month with alloys/25, Industrial produces ~0.16 VP/month — competitive with Housing's ~0.08 VP/month via pop growth
- [x] **Event toast notification HUD (game-designer R17-6):** Add a toast notification system to app.js. Toast container: fixed position, right side, below resource bar (top: 80px, right: 10px). Toast card: 220px wide, dark background (rgba(26,26,46,0.9)), icon + text, CSS slide-in animation from right. Auto-dismiss after 4 seconds with fade-out. Max 5 visible (oldest removed when 6th arrives). Color coding: green border for positive events (constructionComplete, popMilestone, researchComplete, districtEnabled), yellow for warnings (queueEmpty, housingFull), red for crises (foodDeficit, districtDisabled). Wire all existing gameEvent types in the gameEvent message handler to call `_showToast(text, type)`. Add event-specific text formatting: "Construction complete: [districtType] on [colonyName]", "Population milestone: [pops] pops on [colonyName]", etc.
- [ ] **BALANCE TWEAK — T2 tech cost increase (game-designer R18):** Increase T2 tech costs from 500 to 750 research in TECH_TREE (advanced_reactors, gene_crops, deep_mining). At 6 research/month from 1 Research district, T2 now completes in ~125 months (~21 min) instead of ~83 months (~14 min). Makes T2 a real late-game investment in 20-minute matches rather than an automatic completion. Update TECH_TREE costs in game-engine.js and TECH_TREE_UI costs in app.js. Update any tech cost tests
- [ ] **BALANCE TWEAK — Starting alloys and influence (game-designer R18):** Reduce starting alloys from 50 to 0 (no sink exists yet). Reduce starting influence from 100 to 50 (no spend exists yet, 50 preserves budget for future edict system). Update `_initPlayerStates` in game-engine.js. Update all tests referencing starting alloy or influence values
- [x] ~~**PRIORITY ORDER (game-designer R18):** Superseded by R19 priority order. Alloy VP fix and output bumps are done~~
- [x] ~~**PRIORITY ORDER (game-designer R19):** Superseded by R20 priority order. Toast HUD is done~~
- [x] ~~**PRIORITY ORDER (game-designer R20):** Superseded by R21 priority order. Colony ships moved up before fog of war~~
- [x] ~~**PRIORITY ORDER (game-designer R21):** Superseded by R23 priority order. Game speed controls shipped~~
- [x] ~~**PRIORITY ORDER (game-designer R23 — Minimum Viable 4X Loop):** Superseded by R25 priority order. Planet type bonuses shipped~~
- [x] ~~**PRIORITY ORDER (game-designer R25 — Break Open the Galaxy):** Superseded by R31 priority order. Science ships, fog of war, chat, scoreboard, event ticker, colony ships all shipped~~
- [x] ~~**PRIORITY ORDER (game-designer R31 — Deepen the Loop):** Superseded by R34 priority order. Colony traits, T3 techs, crises all shipped~~
- [x] ~~**PRIORITY ORDER (game-designer R37 — The Influence Economy and the Late-Game Cliff):** Superseded by R38 priority order. Influence income shipped~~
- [x] ~~**PRIORITY ORDER (game-designer R38 — The Solitaire Problem):** Superseded by R40 priority order. VP rebalance and scarcity seasons shipped~~
- [x] ~~**PRIORITY ORDER (game-designer R40 — From Colony Sim to Space Opera):** Superseded by R41 priority order. NPC raider fleets shipped~~
- [x] ~~**PRIORITY ORDER (game-designer R41 — The Missing Player Loop):** Superseded by R43 priority order. Corvettes and live scoreboard shipped~~
- [x] ~~**PRIORITY ORDER (game-designer R43 — The Exterminate Gap):** Superseded by R44 priority order. Fleet combat resolution shipped~~
- [x] ~~**PRIORITY ORDER (game-designer R44 — The Multiplayer Awakening):** Superseded by R45 priority order. Ship maintenance shipped~~
- [x] ~~**PRIORITY ORDER (game-designer R45 — The Guns-and-Butter Inflection):** Superseded by R46 priority order. Colony occupation shipped~~
- [x] ~~**PRIORITY ORDER (game-designer R46 — The Occupation Inflection):** Superseded by R47 priority order. Colony procedural naming shipped~~
- [x] ~~**PRIORITY ORDER (game-designer R47 — The Diplomacy Horizon):** Superseded by R48 priority order. Diplomatic stances shipped~~
- [x] ~~**PRIORITY ORDER (game-designer R48 — The Social Contract):** Superseded by R49 priority order. Doctrine choice shipped~~
- [x] ~~**PRIORITY ORDER (game-designer R49 — The Endgame Question):** Superseded by R50 priority order. Endgame crisis shipped~~
- [ ] **PRIORITY ORDER (game-designer R50 — The Three Pillars Stand):** Build order for `/develop`: **(1) Corvette variants via tech (Phase 5, R50-1)** — Interceptor (8HP/5atk, Physics T2), Gunboat (15HP/4atk, Engineering T2), Sentinel (12HP/3atk+2regen, Society T2) — rock-paper-scissors fleet composition, **(2) Underdog production bonus (Phase 6, R50-3)** — +15% production per colony gap vs leader (cap +45%), prevents death spirals, **(3) Distinct victory conditions (Phase 7, R50-2)** — Scientific/Military/Economic instant-win paths, **(4) VP formula rebalance (Phase 1, R49-4)** — battle VP 5→3, survey VP surveyed/3, colony-founded VP +5/colony, alloy VP alloys/20, **(5) Cease-fire negotiations (Phase 6, R50-5)** — propose after 600 ticks of war, +3 VP Peace Dividend on acceptance, **(6) Galactic news ticker (Phase 7, R50-6)** — client-only flavor text on existing events, zero server effort, **(7) Colony ship cost/time reduction (Phase 1, existing)** — 150m/50f/75a cost, 400-tick build time, **(8) Science ship expeditions (Phase 3, R50-4)** — Deep Space Probe/Precursor Signal/Wormhole Mapping one-time missions
- [ ] **Exploration progress indicator (game-designer R31):** Track `player._surveyedCount` and display "X% Explored" badge on galaxy map HUD. Calculate as `surveyedSystems.size / galaxy.systems.length * 100`. Show as small text in galaxy view corner: "12/50 Systems Explored (24%)". Creates a completionist hook and helps players gauge how much galaxy remains undiscovered. Client-side display using data already in gameState (surveyedSystems array). No server changes needed
- [ ] **BALANCE TWEAK — Base capital housing reduction (game-designer R21):** Reduce base capital housing from 10 to 8 in `_calcHousing` (change the `let housing = 10` line to `let housing = 8`). With 8 starting pops and 8 base housing, players hit housing cap immediately and must decide whether to build Housing first (unlock growth) or economic districts first (build income). This diversifies the currently solved opening build order. Update all tests referencing base housing value (search for housing calculations expecting 10). Update `_serializeColony` if it references base housing. Client DISTRICT_UI tooltip for Housing should note "Base: 8 from Capital"
- [x] **Game speed controls (game-designer R19-5):** Add 5 speed settings plus pause. Server-side: add `gameSpeed` to room state (default 3 = 1x). `setGameSpeed` command (host-only in multiplayer, any player in single-player) changes tick interval: speed 1 = 200ms (0.5x), speed 2 = 100ms (1x), speed 3 = 50ms (2x), speed 4 = 33ms (3x), speed 5 = 20ms (5x). `togglePause` command freezes/unfreezes tick loop (host-only in multiplayer). Client: speed indicator in status bar showing current speed level. Keyboard shortcuts: +/= to speed up, - to slow down, Space to toggle pause. Show "PAUSED" overlay when paused. Broadcast speed changes to all clients. Add speed control tests
- [x] **In-game chat panel (game-designer R19-6):** Bring existing lobby chat into the game screen. Add a collapsible chat panel to game-screen in index.html: fixed position bottom-left, 250px wide, max-height 200px, dark translucent background (rgba(26,26,46,0.85)). Show last 20 messages with auto-scroll. Player names colored with their player color from gameState.players. Enter key focuses chat input when game screen is active. Escape unfocuses. Chat input already works via existing WebSocket `chat` message type — just needs a game-screen DOM container and wiring. Reuse Lobby.addChatMessage pattern. Initially collapsed to just an input bar, expands on focus
- [x] **Planet type signature bonuses (game-designer R19-4):** Each habitable planet type gets additive production bonuses applied per working district of matching type in `_calcProduction`. Continental: +1 food per Agriculture district. Ocean: +1 food per Agriculture, +1 research (each type) per Research district. Tropical: +2 food per Agriculture district. Arctic: +1 mineral per Mining, +1 research (each type) per Research district. Desert: +2 mineral per Mining district. Arid: +1 energy per Generator, +1 alloy per Industrial district. Store bonuses in a `PLANET_BONUSES` lookup table in game-engine.js. Apply in `_calcProduction` after tech modifiers. Show bonus in system panel planet table as "+2 Mining" text. Update client DISTRICT_UI tooltips when viewing a specific colony to include planet bonus. Add planet bonus production tests for each type
- [ ] **Cascading colony events (stretch, game-designer R23):** When a colony triggers a crisis event, colonies within 2 hyperlane hops get a related warning event. Plague triggers "Quarantine Warning" on neighbors (pay 20 food to prevent, or 30% chance of plague spreading). Seismic Activity triggers "Aftershock Risk" on neighbors (10% chance of losing 1 district). Creates inter-colony narrative connections. Requires colony crisis events (Phase 2) to be implemented first
- [ ] **BALANCE TWEAK — Generator VP contribution (game-designer R25):** Generators produce no alloys or research, so they earn zero VP. Add generator VP to `_calcVictoryPoints`: count all operational (non-disabled) generator districts across player colonies, add `generators * 1` to VP total (1 VP per generator). Update `_triggerGameOver` breakdown to include `generators` and `generatorsVP` fields. Update client scoreboard to show generator VP. This makes generators a conscious VP choice rather than a mandatory tax. Update VP calculation tests
- [ ] **New district type — Alloy Refinery (game-designer R25):** Add a 7th district type to DISTRICT_DEFS: `refinery: { produces: { alloys: 2 }, consumes: { energy: 2 }, housing: 0, jobs: 1, cost: { minerals: 150, energy: 50 }, buildTime: 350 }`. Cheaper and less energy-intensive than Industrial (4 alloys, 200m, 3 energy) but half the output. Gives early alloy access for colony ship construction without committing to heavy Industrial infrastructure. Add to client DISTRICT_UI with color orange-brown (#b5651d) and appropriate 3D height. Add refinery district tests. Note: do NOT add this if colony ships aren't yet implemented — alloys need a sink first
- [ ] **Add 25-minute match timer option (game-designer R30):** Add 25 to `validTimers` array in `room-manager.js` (currently `[0, 10, 20, 30]` → `[0, 10, 20, 25, 30]`). Update room creation UI to show 25-minute option. 25 minutes is the sweet spot once crises, doctrines, and T3 techs add mid-game depth — long enough for T3 research to land but short enough for competitive tension
- [ ] **BALANCE TWEAK — Starting alloys reduction (game-designer R25, confirmed R27):** Reduce starting alloys from 50 to 25 in `_initPlayerStates`. With colony ships costing 100 alloys, 50 starting alloys lets players produce only 50 more (12.5 months), while 25 requires 75 more (18.75 months). This ~6 second difference makes the Industrial investment decision more deliberate. Update tests referencing starting alloys
- [x] **VP bonus for colony personality traits (game-designer R27):** After implementing the colony personality system (Phase 2), add +5 VP per active colony personality trait to `_calcVictoryPoints`. This rebalances tall vs wide: a 3-colony empire with 3 traits (+15 VP) competes with a 5-colony empire with 0 traits. Include `traitsVP` field in `_triggerGameOver` breakdown. Update VP calculation tests. Requires: colony personality system from Phase 2
- [x] **Ship maintenance costs (game-designer R43-2):** Each corvette costs 1 energy + 1 alloy per month to maintain. Process in `_processMonthlyResources` after colony production: iterate `_militaryShips` per player via `_militaryShipsByPlayer`, sum maintenance (corvetteCount × 1 energy, corvetteCount × 1 alloy), deduct from resources. If energy or alloys go negative from maintenance, degrade all player corvettes by 2 HP each. Ships at 0 HP are destroyed (call `_removeMilitaryShip`). Colony ships cost 1 energy/month while idle (not in transit). Science ships cost 1 energy/month while idle. Show fleet maintenance cost as negative income in resource bar and in corvette build tooltip. Creates guns-vs-butter tension — large fleets drain the economy. 10 corvettes = 10 energy + 10 alloys/month, equivalent to ~2 Industrial districts of alloy income consumed. Add ship maintenance tests
- [ ] **BALANCE TWEAK — VP formula rebalance for military, exploration, and economy (game-designer R45-7, updated R47-4):** Reduce `FLEET_BATTLE_WON_VP` from 5 to 3 in game-engine.js constants. Change survey VP formula from `Math.floor(surveyed / 5)` to `Math.floor(surveyed / 3)` in `_calcVPBreakdown`. Add colony-founded VP: `coloniesVP = colonyCount * 5` as new field in `_calcVPBreakdown` (count from `_playerColonies`). Change alloy VP formula from `Math.floor(alloys / 25)` to `Math.floor(alloys / 20)` — rewards economic stockpiling. Net effect: military aggression still rewarded but doesn't dominate; exploration, expansion, and economy become competitive VP sources. Three viable strategies: military (fight + occupy), economic (tall colonies + traits + alloys), explorer (survey + expand). Update `_calcVPBreakdown` to include `colonies`, `coloniesVP` fields. Update VP breakdown in `_triggerGameOver`. Update client scoreboard to show colony-founded VP and updated exploration VP. Update all VP calculation tests
- [ ] **District adjacency bonus (stretch):** Districts adjacent to another district of the same type get +10% output bonus. Adjacency is defined as sharing a grid edge (not diagonal) in the 4-column layout: left/right neighbors (same row, ±1 col) and above/below neighbors (±1 row, same col). Modify `_calcProduction` to check each working district's neighbors — if any neighbor shares the same type, multiply that district's output by 1.1. Track adjacency count in colony serialization for UI display. Creates a spatial optimization puzzle on top of type selection. Inspired by Anno 1800 production chain layout bonuses. Add adjacency bonus tests

### Phase 2: Colony Management
- [ ] District system: 6 district types — Housing (provides housing for 5 pops, costs 100 minerals, consumes 1 energy/month), Generator (produces 6 energy, costs 100 minerals), Mining (produces 6 minerals, costs 100 minerals), Agriculture (produces 6 food, costs 100 minerals), Industrial (produces 3 alloys, costs 200 minerals, consumes 3 energy/month), Research (produces 3 research each type, costs 200 minerals 20 energy, consumes 4 energy/month). Districts occupy grid slots. Max districts = planet size (8-20). Build time: 300 ticks (30 seconds). First 3 player-built districts on newly colonized planets build at 50% time (150 ticks)
- [ ] Population system: pops live in housing (1 pop per housing unit). Pops work district jobs (1 pop per district). Unemployed pops produce 1 research each. Pop growth: +1 pop every 600 ticks (1 minute) if food surplus > 0. Growth halts if food deficit. Pops consume 1 food each per month-equivalent (100 ticks). If food deficit, random pop dies every 200 ticks
- [ ] Building system: buildings occupy building slots (separate from district grid). Unlock building slots at pop thresholds (slot 1 at 5 pops, slot 2 at 10, etc., max 12 slots). Building types: Administrative Center (capital building, +2 influence, cannot be demolished), Research Lab (+5 physics research, 150 minerals), Engineering Bay (+5 engineering research, 150 minerals), Cultural Center (+5 society research, 150 minerals), Hydroponics Bay (+10 food, 100 minerals), Alloy Foundry (+5 alloys, 150 minerals 50 energy), Civilian Shipyard (enables colony ship production, 200 minerals 100 alloys)
- [ ] Colony overview UI panel: show colony name, planet type, district grid with built/available slots, building slots, pop count and jobs, local resource production breakdown. Click district slot to build, click building slot to build. Show construction progress bars
- [ ] Colony list sidebar: list all player colonies with summary (name, pops, production). Click to switch colony view. Highlight capital colony
- [ ] Construction queue: each colony has a build queue (max 3). Building takes time based on type. Show queue in colony panel with progress and cancel buttons
- [ ] **Construction queue QoL:** Show total mineral cost of all queued items in the build menu so players know remaining budget. Warn (yellow highlight) if queuing a build when the resource stockpile will go negative. Allow cancellation of queued items with 50% resource refund (floor-rounded). Show estimated completion time for each queue item in seconds (ticksRemaining / tickRate). In handleCommand 'demolish', add support for cancelling build queue items by ID (not just built districts)
- [x] Colony personality system: when a colony has 4+ districts of the same type, it earns a trait — "Academy World" (4+ Research: +10% research empire-wide), "Forge World" (4+ Industrial: +10% alloy production empire-wide), "Mining Colony" (4+ Mining: +10% mineral production empire-wide), "Breadbasket" (4+ Agriculture: +10% food empire-wide), "Power Hub" (4+ Generator: +10% energy empire-wide). Show trait badge on colony list and colony view. Only one trait per colony (highest count wins). Empire bonuses stack across colonies (2 Forge Worlds = +20% alloys)
- [x] **Edict system (influence spending):** Add 4 empire-wide edicts that spend influence for temporary bonuses. "Mineral Rush" (50 influence, +50% mining output for 5 months), "Population Drive" (75 influence, +100% pop growth speed for 5 months), "Research Grant" (50 influence, +50% research output for 5 months), "Emergency Reserves" (25 influence, instantly grants +100 energy, +100 minerals, +100 food). Track in playerState: `activeEdict: { type, monthsRemaining }` (max 1 at a time). Process in monthly tick: decrement duration, apply modifiers in `_calcProduction`. Add `activateEdict` command handler. No influence production yet — starting 100 is entire budget until diplomacy. Add edict tests
- [ ] Planet grade rating: calculate and display a D-through-S grade for each planet combining size + habitability + type bonus. Show in system view and colonization target list to help players prioritize. S = size 18+ with 80%+ habitability and strong bonus, D = size 8-10 with 40% habitability and no bonus
- [ ] **Tech complete visual celebration:** When a technology finishes researching, briefly flash all affected districts on the colony grid — e.g., Improved Power Plants completion makes all Generator districts glow bright yellow (emissive intensity 1.0) for 2 seconds, then fade back to normal. Use Three.js emissive material animation on a `researchComplete` gameEvent. Small juice that makes tech completion feel rewarding in the isometric view
- [x] **Mini tech tree (early deliverable):** Implement a lightweight 2-tier, 3-track tech tree as an immediate research sink before the full Phase 4 tech system. Physics Tier 1: Improved Power Plants (+25% Generator output, cost 150 research). Society Tier 1: Frontier Medicine (+25% pop growth speed, cost 150). Engineering Tier 1: Improved Mining (+25% Mining output, cost 150). Physics Tier 2: Advanced Reactors (+50% Generator output, cost 500, requires T1). Society Tier 2: Gene Crops (+50% Agriculture output, cost 500, requires T1). Engineering Tier 2: Deep Mining (+50% Mining output, cost 500, requires T1). Track active research per player: `playerState.currentResearch = { physics: techId, society: techId, engineering: techId }`. Each tick, deduct from accumulated research. On completion, apply multiplicative modifier to `_calcProduction`. Add `setResearch` command handler. Store completed techs in `playerState.completedTechs[]`. Send `researchComplete` event. This makes Research districts immediately valuable without waiting for the full Phase 4 implementation
- [x] **PRIORITY: Influence income from colonies (game-designer R37-1):** Add renewable influence income so edicts are usable throughout the game instead of being a one-shot starter bonus. Each colony generates +2 influence/month base income (capital building). Each active colony personality trait generates +1 influence/month bonus. Total for 3 colonies with 2 traits: 3×2 + 2×1 = 8 influence/month. At 8/month, a 50-cost Mineral Rush edict takes ~6 months (~1 min) to save for — meaningful pacing. Implementation: in `_processMonthlyResources`, after colony production processing, iterate `_playerColonies` for each player, add `2 * colonyCount` base influence, then add `1 * traitCount` (count colonies where `_calcColonyTrait(colony)` is non-null). Cap influence at 200 to prevent late-game stockpiling. Update influence display in client HUD to show income. Add influence income tests covering: base income from 1/2/3 colonies, trait bonus income, cap at 200, no income for 0 colonies
- [ ] **Colony reinforcement (alloy sink):** Add `reinforceColony` command: spend 100 alloys to gain +5 housing on a colony (represents building upgrades/fortification). Max 3 reinforcements per colony (+15 housing total). Track as `colony.reinforcements` counter. Gives alloys an immediate purpose before ships exist. Show reinforcement count in colony panel. Add tests for reinforcement limits and housing calculation
- [ ] **Colony upkeep scaling (game-designer R42):** Add per-colony energy upkeep that scales: Colony 1 = 0 energy/month, Colony 2 = 5/month, Colony 3 = 10/month, Colony 4 = 20/month, Colony 5 = 35/month (total 70 energy/month for 5 colonies). Apply during `_processMonthlyResources` after per-colony production using `COLONY_UPKEEP = [0, 5, 10, 20, 35]` lookup. Display upkeep cost in colony ship tooltip ("Maintenance: +X energy/month"). Creates wide vs tall trade-off — each additional colony is a real economic decision. Add colony upkeep tests
- [ ] **Crisis prevention via specialization (game-designer R42):** If a colony has 4+ districts of a matching type, certain crises are prevented: 4+ agriculture → immune to plague, 4+ generator → immune to power surge, 4+ mining → immune to seismic. Labor unrest is never preventable (social). Check in `_triggerCrisis` — if crisis type is prevented by district count, reschedule instead. Creates a second reason to specialize beyond traits. Add crisis prevention tests
- [x] **Catch-up mechanics: underdog production bonus (game-designer R42):** Players with fewer colonies than the leader get +25% production per colony gap (cap at +50%). Calculate in `_processMonthlyResources`: find max colony count across all players, compute deficit. Apply multiplier to all production. Players in last place by VP also get +2 VP/month, second-to-last +1 VP/month (applied monthly in `_processMonthlyResources`). Show "Underdog Bonus" indicator in resource panel. Add underdog bonus tests
- [x] **PRIORITY: Scarcity seasons — galaxy-wide resource pressure (game-designer R39-6):** Every 800-1200 ticks (80-120 seconds, randomized), one resource gets galaxy-wide -30% production for 300 ticks (30 seconds). 100-tick advance warning broadcast before it starts. Resources: energy/minerals/food (not alloys/research — those are strategic, not commodity). Rotate so same resource doesn't hit twice in a row. Track `this._activeScarcity = { resource, ticksRemaining }` and `this._lastScarcityResource` in game-engine. Apply 0.70 multiplier in `_calcProduction` for matching resource when active. Broadcast `scarcityWarning` (100 ticks before start), `scarcityStarted`, `scarcityEnded` events. Warning gives players time to stockpile or activate Emergency Reserves edict. Invalidate production caches on scarcity start/end. Add scarcity season tests covering: multiplier application, resource rotation, warning timing, cache invalidation, interaction with edicts
- [ ] **Colony established bonus (game-designer R34):** When a colony ship founds a new colony, auto-build 1 Mining district instantly (no build time, no cost — represents materials from the colony ship). Add to `_foundColonyFromShip` after `_createColony`: call `_addBuiltDistrict(colony, 'mining')`. Reduces dead time between founding and productivity by ~30 seconds. New colonies still start weak (2 pops, 1 mining) but aren't completely inert. Add test for new colony having 1 pre-built mining district
- [x] **VP formula rebalance — diminishing pop returns (game-designer R34):** Change VP formula for pops from flat `pops × 2` to diminishing returns: first 20 pops = ×2 VP each, pops 21-40 = ×1.5 VP each (rounded), pops 41+ = ×1 VP each. Increase colony trait VP from +5 to +10 per trait. Increase T3 tech VP from +20 to +30 each. Add exploration VP: +1 VP per 5 systems surveyed (track `surveyedSystems.size`). Update `_calcVPBreakdown` with new formula. Update scoreboard breakdown to show exploration VP. This enables multiple viable strategies (wide/tall/explorer/tech rush). Update all VP tests
- [ ] **Scarcity seasons (moved from Phase 7):** Every 8-12 months (randomized), one resource gets a galaxy-wide scarcity modifier (-25% production for 3 months). Apply in `_processMonthlyResources` as a multiplier. Broadcast scarcity start/end as gameEvent. Announce via event ticker: "ALERT: Mineral scarcity detected across the sector — mining output reduced 25% for 3 months." Rewards diversified economies, punishes over-specialization. Track `this._activeScarcity = { resource, monthsRemaining }` in game-engine. Pick from energy/minerals/food/alloys (not research). On scarcity start, multiply relevant district production by 0.75. On end, remove multiplier. Add scarcity tests
- [ ] **Opening Hands (starting condition draft):** At game start, present each player with 3 randomly-selected starting conditions to choose from. Options pool: "Industrial Start" (+200 alloys, 1 pre-built Industrial district), "Frontier Start" (+100 influence, planet size -2 but +1 colony ship token for later), "Research Rush" (+500 physics research, 1 pre-built Research district), "Mining Boom" (+200 minerals, 1 extra Mining district), "Population Boom" (+4 starting pops, +1 Housing district), "Energy Surplus" (+200 energy, 1 extra Generator), "Explorer's Charter" (1 free science ship spawned at home system, 3 nearest systems pre-surveyed). Server sends `startingOptions` message with 3 random picks. Player sends `selectStartingCondition` with choice index. 30-second timer — if no choice, first option auto-selected. Apply bonuses in `_initStartingColonies`. Add tests for each starting condition
- [ ] **Colony governors (stretch):** Assign a randomly-generated governor NPC to each colony with a personality trait providing a small bonus and a quirk. Pool of 8 governors: "Admiral Chen" (+10% alloys, occasionally demands 50 minerals), "Dr. Voss" (+15% research, -10% pop growth), "Commissioner Hart" (+10% energy, +10% influence, -5% minerals), etc. Governor assigned on colony founding. Track `colony.governor = { name, trait, bonus, quirk }`. Apply bonuses in `_calcProduction`. Quirks trigger as random events every 5-10 months. Show governor portrait/name in colony panel. Adds narrative personality to colonies
- [ ] **Galactic market (stretch):** Shared resource market where players buy/sell energy, minerals, food, alloys at fluctuating prices. Base prices: energy=1, minerals=1, food=1, alloys=3. Price adjusts ±5% per 100 units traded (buy increases price, sell decreases). Track `this._marketPrices` in game-engine, recalculate each month. Add `marketBuy` and `marketSell` commands (min 10 units per trade). Broadcast price changes as gameEvent. Creates economic interconnection without formal diplomacy. UI: market panel showing current prices with trend arrows
- [ ] **Colony mood system:** Colonies have a mood state (Thriving/Content/Restless/Rebellious) calculated from housing ratio (pops/housing), food surplus, and district variety (number of distinct district types). Thriving (housing ratio < 0.7, food surplus > 5, 4+ district types): +10% all output. Content (default): no modifier. Restless (housing ratio > 0.9 OR food deficit): -10% output, emit `colonyRestless` event. Rebellious (housing ratio > 1.0 AND food deficit for 3+ consecutive months): -25% output, after 5 months a random district is destroyed and mood resets to Restless. Track `colony.mood`, `colony.consecutiveDeficitMonths`. Calculate mood in `_processMonthlyResources`. Show mood indicator prominently in colony panel with color coding (green/white/yellow/red). Inspired by Tropico satisfaction — creates a nurturing relationship with colonies rather than pure optimization
- [ ] **Colony legends (narrative history):** Colonies accumulate one-line history entries on milestones: "Founded by [player] on Month 1", "Survived the Great [Resource] Scarcity of Month [N]", "Became the empire's first [Trait] World on Month [N]", "Reached [N] pops on Month [N]". Track as `colony.legends = []` array of `{ month, text }`. Generate entries on: colony founding, surviving a scarcity season, earning a personality trait, pop milestones (10/20/30), colony reaching full development. Show legends in colony info panel as scrollable timeline. At game end, display each colony's legend as a narrative recap on the post-game screen. Max 20 entries per colony to prevent bloat. Adds narrative flavor and makes each match feel like a unique story. Inspired by Dwarf Fortress legends mode
- [ ] **Contested colonization race resolution (game-designer R28):** When two colony ships from different players target the same planet, the first to arrive wins. If ships arrive on the same tick, resolve by coin flip (seeded random). The losing ship is removed, and the owner receives a 50% resource refund (100 minerals, 50 food, 50 alloys) credited to `playerState.resources`. Emit `colonyShipRaceWon` event to winner and `colonyShipRaceLost` event to loser with system name and refund details. Check in `_foundColonyFromShip`: before founding, verify `planet.colonized === false`. If another ship already colonized it this tick, trigger the race resolution. Add race resolution tests
- [x] **Science ship unit type (game-designer R28):** Cheaper, faster exploration unit buildable from colony build queue. Cost: 100 minerals + 50 alloys. Build time: 300 ticks (30 sec). Speed: 30 ticks/hop (3 sec, faster than colony ships). Max 3 per player. When a science ship arrives at an unexplored system, it automatically surveys for 100 ticks (10 sec). Survey reveals full planet data and has 20% chance per planet of discovering an anomaly. Anomalies are simple one-time bonuses: "Ancient Ruins" (+50 research each track), "Mineral Deposit" (+100 minerals), "Habitable Moon" (+2 planet size), "Precursor Artifact" (+25 influence), "Derelict Ship" (+50 alloys). Track as `_scienceShips[]` alongside `_colonyShips[]`. Surveyed systems remember surveyed state per player (persistent fog penetration — stays revealed even outside 2-hop range). Science ships return to nearest colony after surveying. Render as cyan diamond on galaxy map (vs green for colony ships). Survey progress visible as loading bar when hovering system. Add `buildScienceShip` and `sendScienceShip` commands. Creates "scout then settle" loop that adds planning layer to expansion
- [ ] **Planetary features as tile modifiers (stretch, game-designer R28):** When founding a colony, generate 2-4 special tiles on the colony grid with bonus modifiers. "Geothermal Vent" (+3 energy if generator built here), "Rare Crystal Deposit" (+2 research per track if research built here), "Fertile Valley" (+3 food if agriculture built here), "Ore Seam" (+3 minerals if mining built here), "Ancient Ruins" (one-time +50 research when first district built on tile). Track as `colony.features = [{ tileIndex, type, bonus }]`. Render as subtle glow/icon overlay on colony grid tiles. Feature bonuses apply on top of district production. Creates spatial puzzles: "should I build my research district on the crystal deposit or save that tile for later?" Makes each colony layout unique. Reference: Civ VI adjacency bonuses
- [x] **Crisis interval scaling (game-designer R33):** Increase crisis cooldown by +100 ticks per colony beyond 3 to prevent late-game micro fatigue. In `_scheduleCrisis`, calculate `const colonyCount = this._playerColonies.get(colony.ownerId)?.length || 1; const extraDelay = Math.max(0, colonyCount - 3) * 100;` and add `extraDelay` to the next crisis tick. A 5-colony empire gets +200 ticks between crises (~20 extra seconds), reducing the crisis rate from one every 16-22 seconds to one every 36-42 seconds. This prevents the late-game "whack-a-mole" feeling while keeping crises meaningful. Update crisis scheduling tests
- [ ] **Resource deficit projection warning (game-designer R32):** Client-side UX improvement. When a player's projected net production (current production minus current consumption, accounting for all in-queue buildings completing) would result in a deficit for any resource, show a warning indicator next to that resource in the HUD panel. Amber icon if deficit would occur only after queue completion; red icon if currently in deficit. Tooltip text: "Energy: +6/month now, -1/month when Industrial completes." Calculate projected net in client `_updateHUD()` by iterating colonies' buildQueue, summing the production/consumption of queued district types using DISTRICT_DEFS data sent in gameInit. No server changes needed — purely client-side projection. Prevents frustrating surprise deficits and makes resource management feel strategic rather than punishing. Reference: Stellaris resource projection, Anno production warnings
- [x] **Colony crisis events (4 types, game-designer R30 priority #1):** Every 500-800 ticks (randomized per colony), trigger a crisis requiring choice within 200 ticks (20 sec). "Seismic Activity": Evacuate (lose district, save pops) or Reinforce (spend 100 minerals, 30% failure → lose district + 1 pop). "Plague Outbreak": -1 pop per 100 ticks; Quarantine (growth halted 300 ticks, no pop loss) or Rush Cure (50 energy + 50 food, 80% success, 20% spreads to another colony). "Power Surge": Shut Down (all districts disabled 100 ticks) or Ride It Out (+50% energy 200 ticks, 25% chance lose generator). "Labor Unrest": 3 random districts disabled; Negotiate (spend 25 influence, resume immediately) or Wait (strike ends in 300 ticks). Track `colony.crisisState = { type, ticksRemaining, resolved }`, `colony.nextCrisisTick`. Add `resolveCrisis` command with choice parameter. Unresolved crises auto-resolve worst outcome. Crisis immunity 300 ticks after resolution. Broadcast crisis events to all players. Add crisis tests

### Phase 3: Galaxy & Exploration (**CURRENT PRIORITY — game-designer R15, R17, R22**)

**Build order (R22 update):** Colony ships + colony list UI → Planet type bonuses → Fog of war → In-game chat → Military outposts → Surface anomalies → Science ships + surveying → Starbases → Territory visibility. Colony ships are the watershed — everything else compounds with them.

- [x] Procedural galaxy generation: on game start, generate N star systems (small=50, medium=100, large=200) as 3D points. Use Poisson disc sampling for even distribution. Connect nearby systems with hyperlanes (Delaunay triangulation, then prune to avg 3-4 connections per system). Each system has: name (generated), star type (yellow/red/blue/white), 1-6 planets
- [x] Planet generation per system: each planet has: orbit slot (1-6), size (8-20 district slots), type (Continental, Ocean, Arctic, Desert, Arid, Tropical, Barren, Molten, Gas Giant), habitability (0-100% based on type — Continental/Ocean/Tropical 80%+, Arctic/Desert/Arid 60%, Barren 0%, Gas Giant 0%). Resource modifiers per type (e.g., Desert = +minerals, Ocean = +food, Barren = +minerals but uninhabitable). Only habitable planets (>20% habitability) can be colonized
- [x] **PRIORITY (game-designer R17-1): Galaxy map view (Three.js):** Create `src/public/js/galaxy-view.js` module. PerspectiveCamera scene with OrbitControls (damping enabled). Stars as small emissive MeshBasicMaterial spheres (radius 1.5-3.0 scaled by star type), colored by STAR_TYPES from galaxy.js (yellow=#f9d71c, red=#e74c3c, blue=#3498db, white=#ecf0f1, orange=#e67e22). Hyperlanes as THREE.LineSegments with BufferGeometry (white, opacity 0.3). Player-owned systems get a colored ring/halo (player color from PLAYER_COLORS). Camera starts above galaxy center at ~45° looking down. System name labels on hover using CSS2DRenderer or sprite text. Store galaxy data from `gameInit` in client module. Click system to select (highlight + show name). No server changes needed — galaxy data already sent in gameInit
- [x] **PRIORITY (game-designer R17-7): System selection panel on galaxy map:** When a system is clicked on galaxy map, show a right-side panel (reuse game-panel CSS class) with: system name, star type label, planet list table (orbit#, type, size, habitability%). If system has a player colony, show colony name and "View Colony" button that switches to colony view. If system is unsurveyed by this player, show "Unsurveyed" placeholder. Panel hides when clicking empty space. Reuse existing panel hide/show pattern from district-info panel
- [ ] **PRIORITY: System orbital view (game-designer R24-1):** Build `src/public/js/system-view.js` as the bridge between galaxy and colony views. Central star as large emissive sphere (colored by star type, radius 8-12). Planets on concentric orbital rings (THREE.RingGeometry dashed or LineLoop circles), each planet as a colored sphere sized by `planet.size * 0.3` (range ~2.4 to 6.0 radius). Planet colors by type: Continental=#2ecc71, Ocean=#2980b9, Tropical=#27ae60, Arctic=#ecf0f1, Desert=#e67e22, Arid=#d35400, Barren=#7f8c8d, Molten=#c0392b, Gas Giant=#f39c12 (2-3x larger). Habitable planets get a subtle translucent atmosphere sphere (slightly larger, opacity 0.15). Camera: PerspectiveCamera, slight top-down angle (~60° from horizontal), orbit controls for rotation/zoom. Planets orbit slowly (cosmetic, ~0.001 rad/tick). Click planet → shows detail panel (type, size, habitability, bonus, "Colonize" button if colonized). Double-click star in galaxy view OR click "View System" button in system panel → transition to system view. Escape/Back button returns to galaxy. Load system view script in index.html. Wire into app.js view switcher alongside galaxy/colony views. Navigation flow: Galaxy → System → Colony
- [ ] **Colony planet context rendering (game-designer R24-2):** In colony-view.js (renderer.js), change the ground plane color and scene background based on colony planet type. Ground plane MeshStandardMaterial colors: Continental=#3d6b35, Ocean=#1a3a5c, Tropical=#2d5a27, Arctic=#b8c9d4, Desert=#c4956a, Arid=#8b5e3c. Scene background (skybox substitute): Continental=#0a1a2a, Ocean=#050f1a, Tropical=#0a1a0f, Arctic=#101820, Desert=#1a140a, Arid=#1a100a. Add a horizon glow using a large hemisphere or gradient plane behind the colony grid with planet-appropriate atmosphere tint (opacity 0.1-0.2). Apply colors in `buildColonyGrid()` based on colony.planet.type from gameState. Makes each colony visually distinct — Desert colonies feel sandy, Arctic colonies feel icy
- [ ] **Planet visual polish in system panel (game-designer R24-3, quick win):** Enhance the existing HTML system panel planet table. Add colored circle dot before planet type name (CSS `::before` pseudo-element or inline span with background-color matching planet type colors). Bold the Size column for planets size 15+. Add gold/highlight border on the "best" planet row (highest habitability * size product). Add planet type color as text color for the Type column. Already has hab-high/hab-med/hab-none CSS classes — extend with planet-type-specific colors
- [x] **Fog of war on galaxy map (game-designer R17-4, R23 MVL-3):** Three visibility tiers calculated client-side on each gameState update. **Known** (within 2 hyperlane hops of any owned system or outpost): full-color star, name on hover, hyperlanes shown as solid lines, planet details visible in system panel. **Unknown** (everything else): dim gray dot (opacity 0.2), no name on hover, no planet data in system panel, hyperlane connections shown as faded dashed lines. Starting system and its direct neighbors always known. Other players' colonies visible as colored ownership dots (but no planet details unless within your known range). Implementation: in galaxy-view.js, build adjacency list from `galaxy.hyperlanes`. On each state update, BFS from all owned system IDs (from colonies array where ownerId === myId) out to depth 2. Store result as `knownSystemIds` Set. When rendering stars: if system.id in knownSystemIds, render full; else render dim. When rendering hyperlanes: if both endpoints known, render solid; if one known, render faded; if neither, render very faint or hide. System panel click: if unknown, show "Unexplored — send a colony ship to learn more" instead of planet list. No server changes needed — all client-side rendering
- [ ] **PRIORITY: Science ship expeditions with risk/reward (game-designer R43-4):** After surveying 5+ systems, unlock "Expeditions" — science ships can be sent on timed missions to distant unsurveyed systems for escalating rewards. `sendExpedition` command: requires idle science ship + target unsurveyed system 3+ hops away from any owned system. Three expedition types (player chooses): **Deep Space Probe** (600 ticks/60s, guaranteed +200 of random resource, +3 VP), **Precursor Signal** (900 ticks/90s, 70% chance +500 research across all tracks, 30% chance ship lost, +5 VP on success), **Wormhole Mapping** (600 ticks/60s, creates temporary instant-travel link between any 2 player-owned systems for 1200 ticks/2 min, +2 VP). Ship unavailable during expedition (set `ship.expeditioning = true`, `ship.expeditionType`, `ship.expeditionProgress`). Process in `_processScienceShipMovement`: increment progress, on completion apply reward, mark system `expeditioned = true`, return ship to nearest colony. Max 1 active expedition per science ship. Fills the mid-game dead zone (minutes 8-15) with active risk/reward decisions. Gives science ships ongoing utility beyond early-game surveying. Add expedition type, progress, completion, ship-loss, and VP tests
- [ ] **Science ship expeditions — re-exploration system (game-designer R41-6):** After initial galaxy survey, science ships can run "expeditions" at already-surveyed systems. New command: `runExpedition` — requires idle science ship at a surveyed system. Expedition takes 200 ticks (20 seconds). Each system can only be expeditioned once (track `system.expeditioned = true` on galaxy system objects). Reward pool (weighted random): 40% research bonus (+100 in random track), 25% alloy cache (+50 alloys), 20% influence gain (+15 influence), 15% hidden anomaly (reroll from ANOMALY_TYPES). Emit `expeditionComplete` event with reward details. +2 VP per expedition completed in `_calcVPBreakdown` (add `expeditions` and `expeditionsVP` fields). Process in `_processScienceShipMovement` alongside survey logic — when ship is at a surveyed, non-expeditioned system and `ship.expeditioning = true`, increment `ship.expeditionProgress`. On completion, apply reward, mark system, award VP. This keeps science ships relevant throughout the entire match and gives exploration-focused players sustained activity. Reference: Stellaris archaeological dig sites (simplified for 20-min matches). Add expedition tests
- [ ] **Science ship auto-chain survey (game-designer R31):** After a science ship completes a survey and returns to its colony, automatically target the nearest unsurveyed system within 3 hyperlane hops and begin transit. If no unsurveyed systems within range, ship remains idle at colony. Add `ship.autoSurvey = true` flag (default true). Player can toggle auto-survey off via a UI button on the science ship panel to manually control the ship. Reduces micro-management of exploration — the "set it and forget it" pattern lets players focus on colony management while scouts do their job. Check in `_returnScienceShipToColony` completion: if autoSurvey, find nearest unsurveyed via BFS (max depth 3), auto-dispatch. Add auto-chain tests
- [ ] Fleet fundamentals: science ship (surveys systems, discovers anomalies), colony ship (founds new colonies, consumed on use), construction ship (builds starbases). Ships move along hyperlanes between systems — travel time based on distance (default 5 seconds per hyperlane hop). Fleet movement shown on galaxy map as animated dots along hyperlane paths
- [ ] System surveying: unsurveyed systems appear as "?" on galaxy map. Send science ship to survey — takes 10 seconds per planet in system. Surveying reveals planet details (type, size, habitability, resources). May discover anomalies (20% chance per planet — placeholder for now, just bonus resources)
- [x] **PRIORITY: Minimal colony ship (game-designer R18-8, R23 MVL-2):** Implement colony ships as the simplest expansion path — no Civilian Shipyard prerequisite, buildable directly from colony build queue. Cost: 200 minerals, 100 food, 100 alloys. Build time: 600 ticks (60 sec). On completion, colony ship appears as a movable unit icon at the colony's system on the galaxy map. Click to select, click habitable planet in a known system to send. Movement: 50 ticks (5 sec) per hyperlane hop, pathed along hyperlanes using BFS shortest path. On arrival at target planet, ship consumed, new colony founded with 2 pops on the target planet (new colony uses `_createColony` with `isStartingColony = false` so first-3-districts discount applies). Max 5 colonies per player. Shared resource pool across all colonies (Stellaris model). Track colony ships in `playerState.colonyShips = [{ id, systemId, targetSystemId, path, hopProgress }]`. Add colony ship as special build queue entry (type: 'colonyShip'), `sendColonyShip` command (validates: target planet is habitable with habitability > 20%, target system is within known visibility range, planet not already colonized, player under 5 colony cap). Process movement in tick loop — each tick increment hopProgress, on reaching 50 advance to next system in path. Galaxy view: render colony ships as small animated diamond markers moving along hyperlanes. Colony list sidebar appears when player has 2+ colonies (see Phase 1 colony list sidebar task). This is the single most impactful feature for transforming the game from city-builder to 4X
- [ ] Colonization (full version): build colony ship at Civilian Shipyard (cost: 200 minerals 100 food 100 influence). Send colony ship to habitable surveyed planet. On arrival, colony ship is consumed, new colony founded with 2 starting pops, Administrative Center auto-built. Colony appears in colony list. System claimed for player
- [ ] Starbase construction: send construction ship to a system to build a starbase (cost: 200 minerals 100 alloys). Starbases claim uncolonized systems for the player. Starbases can be upgraded later for defense/economy. One starbase per system
- [x] **Colony founding broadcast and galaxy update:** When a colony ship founds a new colony, broadcast `colonyFounded` gameEvent to all players with colonizer name, system name, and planet type. Update galaxy map ownership rings in real-time. Show toast notification to all players: "Player X has founded a colony in [System Name]!" If fog of war is active, only reveal the system as owned (colored dot) without exposing planet details. Creates multiplayer awareness and territorial tension. Also send `expansionAlert` to players whose owned systems are within 2 hyperlane hops of the new colony
- [ ] Multiplayer territorial visibility: when any player surveys or colonizes a system, all players see the ownership claim on the galaxy map (colored border/flag). Show proximity alerts: "Player X has colonized a system 2 hyperlane hops from your territory." Creates tension without requiring combat
- [ ] Simple resource gifting: allow players to send one-way resource gifts to other players via a "Send Resources" button on the diplomacy panel. No formal trade system needed — just pick resource type, amount, and recipient. Enables informal cooperation through chat + gifts before full diplomacy is implemented
- [ ] Expanded anomaly events (15 total with 3 tiers): Tier 1 common (50%) — Mineral Deposit, Alien Flora, Energy Signature with single-choice rewards. Tier 2 uncommon (35%) — Ancient Ruins, Derelict Fleet, Subspace Anomaly with branching two-step chains and risk/reward trade-offs. Tier 3 rare (15%) — Precursor Homeworld (guarded unique planet), Dimensional Rift (crisis seed), Lost Colony (free colony). Each anomaly has distinct narrative text and meaningful mechanical impact
- [x] **PRIORITY (game-designer R22-5): Colony switcher UI for multi-colony management:** Left sidebar colony list panel, visible when player has 2+ colonies. Each entry: colony name, pop count, status icon (growing/building/starving/idle). Click to switch colony view to that colony. Keyboard shortcuts 1-5 to switch between colonies. Status bar shows "Colony 2/3" indicator. Colony panel title updates to show current colony name. This is required alongside colony ships — multi-colony gameplay needs multi-colony UI
- [ ] **PRIORITY (game-designer R22-6, updated R39-2): Military outpost system (first territorial competition):** Buildable at surveyed, uncolonized systems from galaxy map. Cost: 100 alloys + 25 influence, 200 ticks build time. Effects: claims system (prevents others from colonizing), +1 influence/month income (processed in `_processInfluenceIncome`), extends fog-of-war visibility range by +1 hop. Max 3 outposts per player. Can only build in systems within 2 hops of owned systems. +3 VP per outpost. If another player colonizes a planet in the system, outpost ownership transfers. Raiders (R39-1) targeting a system with only an outpost destroy it (no combat). Track in `playerState.outposts = [{ id, systemId, buildProgress }]`. Add `buildOutpost` command — validates system surveyed, uncolonized, no other player's outpost, player under 3 outpost cap. Render as station icon colored by player on galaxy map. Creates first multiplayer territorial competition and alloy sink. Add outpost tests
- [ ] **Galactic leylines (stretch, game-designer R27):** Hidden resource veins connecting 2-3 star systems, generated alongside the galaxy. Each leyline targets a specific resource (energy/minerals/food/alloys). Generate 3-5 leylines per small galaxy, 6-10 per medium, 10-15 per large. Store as `galaxy.leylines = [{ id, systemIds: [a, b, c], resource, bonus: 0.15 }]`. A leyline is only revealed to a player once they colonize one endpoint system — emit `leylineDiscovered` event with connected system IDs and resource type. If a player controls colonies in ALL systems of a leyline, they get +15% production of the leyline's resource across all colonies (applied in `_calcProduction` as a multiplier). Track active leylines per player in `playerState.activeLeylines[]`. Recalculate on colony founding/loss. Show as glowing colored lines on galaxy map between connected systems (only if discovered). Creates a puzzle layer on expansion — settle the "obviously good" planet or complete a leyline? Add leyline generation, discovery, and bonus tests
- [ ] **Emergent trade routes (stretch, game-designer R25):** When two different players have colonies within 3 hyperlane hops of each other (BFS distance), an automatic trade route forms. Each player in the route gets +5% energy income (applied as multiplier in `_processMonthlyResources`). Visible on galaxy map as a glowing dashed line between the two closest owned systems. Track `this._tradeRoutes = [{ player1, player2, system1, system2 }]` — recalculate on colony founding/loss. Multiple routes between same players don't stack. Creates organic multiplayer economic dynamics and a visual web of connections. Broadcast `tradeRouteFormed` event to both players. Simpler than formal trade negotiations — emergence over negotiation
- [ ] **Planet type bonus visibility in system panel (game-designer R22-2):** In the galaxy map system panel, show planet type bonuses as colored tags next to each planet row. E.g., Desert planet shows "+2 Mining" tag in gray. Arctic shows "+2 Research" in purple. Makes planet bonuses discoverable when scouting the galaxy for colony ship destinations. Uses PLANET_BONUSES definitions from game-engine.js. Client-only rendering change
- [ ] **Precursor anomaly chain (game-designer R42):** Add a 3-phase exploration quest triggered by finding a special "Precursor Signal" anomaly (5% chance on survey, only triggers once per galaxy — track `this._precursorClaimed = false`). Phase 1: finder gets `precursorSignal` event, 3 distant systems marked with golden star indicators on galaxy map (store as `playerState.precursorTargets = [sysId, sysId, sysId]`). Phase 2: player must survey all 3 target systems. Phase 3: on completing all 3 surveys, player chooses reward: +25% all production (permanent multiplier), +50 VP, or +2 max colonies. Broadcast "Player X has found Precursor artifacts!" to all players on Phase 1 discovery. Creates mid-game exploration race. Add precursor chain tests

### Phase 4: Technology & Research
- [x] **BALANCE FIX — Research VP weight (game-designer R29):** Research VP is nearly worthless compared to pop VP. Change VP formula for research from `totalResearch / 100` to `totalResearch / 50` (doubles VP contribution). Additionally, add +5 VP per completed T1 tech, +10 VP per completed T2 tech, +20 VP per completed T3 tech (when T3 exists). Update `_calcVictoryPoints` in game-engine.js. Update `_triggerGameOver` breakdown to include techVP. Update scoreboard display to show tech VP contribution. This makes "tech rush" a viable strategy: a player who researches all 6 techs gets +30 VP bonus (3×5 + 3×10) plus doubled research stockpile VP. Update all VP-related tests
- [x] **PRIORITY: Tech tree T3 expansion (game-designer R17-5):** Add Tier 3 techs to existing mini tech tree (3 new techs). Physics T3: Fusion Reactors (+100% Generator output + generators produce +1 alloy/month, cost 1000, requires advanced_reactors). Society T3: Genetic Engineering (+100% Agriculture output + pop growth time halved, cost 1000, requires gene_crops). Engineering T3: Automated Mining (+100% Mining output + mining districts cost 0 jobs, cost 1000, requires deep_mining). Add to TECH_TREE in game-engine.js with new effect types: `districtBonus` with additional `alloysBonus: 1` for Fusion Reactors, `growthBonus: 0.5` for Genetic Engineering, `jobBonus: { mining: 0 }` for Automated Mining. Update `_calcProduction` and `_processPopGrowth` to handle new effect types. Update client TECH_TREE_UI with 3 new T3 cards. T3 at 1000 cost with 2 research districts (6/month) takes ~167 months (~28 min) — unreachable without heavy research investment, creating a viable "tech rush" strategy. Add T3 tests
- [ ] Tech tree structure: three parallel tracks (Physics, Society, Engineering). Each track has 5 tiers. Each tier has 3-4 tech options. Player researches one tech per track at a time. Research cost scales per tier (tier 1: 500, tier 2: 1000, tier 3: 2000, tier 4: 4000, tier 5: 8000). Research points per tick reduce remaining cost. When cost reaches 0, tech is complete
- [ ] Physics techs: Tier 1 — Improved Power Plants (+25% Generator output), Laser Weapons I (+10% ship weapon damage), Sensor Arrays (+2 survey speed). Tier 2 — Advanced Reactors (+50% Generator output), Laser Weapons II (+20% damage), Hyperspace Mapping (+25% fleet speed). Tier 3-5 — increasingly powerful versions and new unlocks
- [ ] Society techs: Tier 1 — Hydroponics (+25% Agriculture output), Colonial Bureaucracy (+1 building slot all colonies), Frontier Medicine (+25% pop growth). Tier 2 — Gene Crops (+50% Agriculture output), Planetary Administration (+2 building slots), Galactic Ambitions (+50 influence cap). Tier 3-5 — advanced versions
- [ ] Engineering techs: Tier 1 — Improved Mining (+25% Mining output), Alloy Smelting (+25% Industrial output), Corvette Hulls (unlock corvettes). Tier 2 — Deep Mining (+50% Mining output), Advanced Alloys (+50% Industrial output), Destroyer Hulls (unlock destroyers). Tier 3-5 — cruisers, battleships, mega-structures
- [ ] Research UI: panel showing three tracks side by side, current research per track with progress bar, available techs to pick when current completes, tech details on hover (cost, effect, prerequisites). Locked techs shown grayed with prerequisite chain
- [ ] Tech effects system: when a tech completes, apply its modifiers to the player's state. Modifiers are multiplicative (e.g., +25% Generator output means all generator districts produce 5 instead of 4). Track active modifiers per player. Send `researchComplete` message to client with new options

- [ ] **BALANCE TWEAK — T3 tech cost reduction (game-designer R34, updated R41):** Reduce T3 tech cost from 1000 to 750 in TECH_TREE (fusion_reactors, genetic_engineering, automated_mining). With 2 Research districts (8 research/track/month), T3 completes in ~94 months (~16 min) — achievable in the last quarter of a 20-minute match without over-committing to research. Current 1000 cost requires 3 research districts to finish in time, which is too heavy. 800 (R39 recommendation) was still borderline — 750 makes T3 a realistic stretch goal with 2 research districts. Update TECH_TREE costs in game-engine.js and TECH_TREE_UI costs in app.js. Update tech cost tests
- [x] **PRIORITY: Doctrine choice at game start (game-designer R43-6):** At game start (during first 30 seconds), players choose one of 3 doctrines that modify starting bonuses. Selection screen with 30-second timer; random assignment if no choice. **Industrialist:** +25% Mining and Industrial output (apply as multiplier in `_calcProduction` via `playerState.doctrine.type`), start with 1 extra Mining district pre-built, -10% research output. **Scholar:** +25% Research output, start with T1 research 33% complete in all 3 tracks (set `researchProgress[techId] = 50` for each T1), -10% mineral output. **Expansionist:** Colony ships cost 25% less (`COLONY_SHIP_COST` modified per-player) and build 25% faster, start with 10 pops instead of 8, -10% alloy output. Server sends `doctrineSelect` event with 3 options at game start. Player sends `selectDoctrine { doctrineType }`. Track as `playerState.doctrine = { type }`. Apply modifiers in `_calcProduction` (check doctrine type, apply multiplier to relevant resources). Doctrine visible on scoreboard and in player summary. Breaks the solved opening build order — each doctrine suggests different first 3 builds. Add doctrine selection, modifier application, and VP interaction tests
- [ ] **Empire specialization doctrines (game-designer R28):** At 3 colonies, players unlock a permanent doctrine choice (pick one of three, cannot change). **Expansionist**: colony ship build time -40%, colony cap +2 (to 7). **Industrialist**: all district build time -25%, +1 alloy per Industrial district. **Scholar**: research +25% empire-wide, each completed tech grants +3 VP. Server sends `doctrineAvailable` event when player reaches 3 colonies. Player sends `selectDoctrine` command with choice. Track as `playerState.doctrine = { type }`. Apply modifiers in relevant processing functions. Show doctrine badge on scoreboard and colony panel. Doctrine creates asymmetric mid-game strategies with permanent commitment — Expansionist goes wide (7 colonies), Industrialist goes tall (efficient production), Scholar goes tech rush. Add doctrine selection and effect tests
- [ ] **Tech tree branching: exclusive T2 choices (game-designer R42):** Add a second T2 option per track (mutually exclusive with existing T2). Physics T2B: "Shield Technology" (cost 500, +20 HP to all military ships, requires improved_power_plants). Society T2B: "Colonial Administration" (cost 500, +1 max colony, +25% influence income, requires frontier_medicine). Engineering T2B: "Advanced Alloys" (cost 500, +50% alloy output from Industrial districts, requires improved_mining). Add `exclusive` field to TECH_TREE entries linking paired T2s — `setResearch` rejects if exclusive alternative is already completed. Add T3B techs unlocked by each T2B: Physics T3B "Deflector Arrays" (+50% military ship HP), Society T3B "Galactic Governance" (+2 max colonies, edicts cost -50%), Engineering T3B "Nanoassembly" (+100% alloy output, -25% ship build time). Creates 8 possible tech configurations (2³). Add exclusive tech and branching tests

### Phase 5: Fleets & Combat
- [x] **Corvette ship class (game-designer R34):** Add first military ship type. Cost: 100 minerals + 50 alloys. Build time: 400 ticks (40s). Stats: 10 HP, 3 attack. Build via `buildCorvette` command at any colony with starbase. Track as `_militaryShips[]` with `{ id, ownerId, systemId, targetSystemId, path, hopProgress, hp, attack }`. Movement: 40 ticks/hop along hyperlanes (same as colony ships). Max fleet size: 10 corvettes per player. Render as red triangle on galaxy map. Add `sendFleet` command to move corvettes to target system. Add corvette build/movement tests
- [x] **PRIORITY: Fleet combat resolution (game-designer R43-1):** When a corvette's path ends at a system containing enemy military ships, trigger `_resolveFleetCombat(systemId)`. Combat: both sides attack simultaneously each round — each ship deals its `attack` (3) damage to the enemy ship with lowest HP (focus fire). Repeat for up to 10 rounds or until one side eliminated. Surviving ships remain in system. Destroyed ships removed via `_removeMilitaryShip`. VP: +5 VP per fleet battle won (add `battlesWon` to VP breakdown), -2 VP per own ship lost (add `shipsLost` to VP breakdown). System control: a system with your corvettes and no enemy corvettes blocks enemy colonization there. Emit `combatStarted` and `combatResult` events with ship counts, losses, winner. Render combat flash on galaxy map. Retreat command `retreatFleet`: corvettes at a hostile system flee to adjacent system, each enemy corvette gets 1 free attack during retreat. This is the #1 priority — corvettes exist but can't do anything. Add fleet combat tests (equal forces, unequal forces, retreat, VP changes)
- [x] **PRIORITY: Colony occupation after fleet combat (game-designer R43-1b, updated R45-1):** After winning combat in a system with an enemy colony, colony enters "occupied" state. Occupation requires: attacker has corvettes in system AND defender has zero military ships there. Occupation takes 300 ticks (30 seconds) with corvettes present — track `colony.occupationProgress` (0-300), increment each tick while conditions met, reset to 0 if attacker ships leave or defender ships arrive. Once complete: `colony.occupiedBy = attackerPlayerId`. Occupied colonies produce at 50% output (apply 0.5 multiplier in `_calcProduction`). VP: defender loses 5 VP per occupied colony, attacker gains +3 VP per occupied colony (processed in `_calcVPBreakdown`). Liberation: defender moves corvettes to system AND no enemy corvettes remain → `colony.occupiedBy = null`, `colony.occupationProgress = 0`. Add `occupiedBy` and `occupationProgress` to colony serialization. Show occupation status in colony panel (red border, "OCCUPIED" badge with progress bar during takeover). Emit `colonyOccupied`, `colonyLiberated`, and `occupationProgress` events. Add occupation tests covering: progress increment, progress reset, completion, VP changes, liberation, production reduction
- [x] ~~**Basic fleet combat (game-designer R34):** Superseded by R43-1 fleet combat resolution~~
- [ ] **Colony occupation (game-designer R34):** After winning combat in a system with an enemy colony, colony becomes "occupied": produces at 50% output and grants occupier +2 VP per occupied colony. Track `colony.occupiedBy = playerId`. Apply 0.5 multiplier in `_calcProduction` when occupied. Occupation ends if occupier loses all ships in the system. Add `occupied` state to colony serialization. Add occupation tests
- [ ] **Fleet energy maintenance (game-designer R42):** Military ships cost energy/alloys per month to maintain. Corvette: 1 energy/month. Destroyer: 3 energy/month. Cruiser: 6 energy/month. Process in `_processMonthlyResources` after colony production — iterate `_militaryShips` per player, sum maintenance, deduct from resources. If energy goes negative, apply existing energy deficit logic (disables districts). Creates guns-vs-butter tension — large fleets drain the economy. Show fleet maintenance cost in build tooltip and fleet panel. Add fleet maintenance tests
- [ ] Ship classes: Corvette (cost: 50 alloys, 30 HP, 10 firepower, 5 speed, 1 fleet cap), Destroyer (100 alloys, 80 HP, 30 firepower, 4 speed, 2 fleet cap), Cruiser (200 alloys, 200 HP, 80 firepower, 3 speed, 4 fleet cap), Battleship (400 alloys, 500 HP, 200 firepower, 2 speed, 8 fleet cap). Each ship class must be researched first (corvettes available by default)
- [ ] Fleet management: group ships into fleets. Fleet speed = slowest ship. Fleet cap starts at 20 (from starbase), increased by techs and starbases. Fleets move between systems along hyperlanes. Fleet UI: list of fleets with composition, location, orders. Click to select, right-click system to move
- [ ] Shipyard system: build military ships at starbases with shipyard module (upgrade cost: 100 alloys). Queue up to 5 ships. Build time: Corvette 10s, Destroyer 20s, Cruiser 35s, Battleship 60s. Ships spawn at starbase system
- [ ] Space combat resolution: when hostile fleets occupy same system, combat begins. Each tick: each ship fires at random enemy ship (weighted by fleet cap — bigger ships draw more fire). Damage = firepower vs HP. Ships destroyed at 0 HP. Combat continues until one side is eliminated or retreats. Retreating fleet moves to adjacent friendly system at half speed. Send `combatResult` to involved players with losses
- [ ] Starbase defense: starbases have base combat stats (100 HP, 40 firepower). Upgraded starbases are stronger. Starbases fight alongside defending fleets. Destroying enemy starbase removes their system claim
- [ ] Military UI: fleet list panel, fleet composition view, ship build queue at starbases, combat log/notifications. "Fleet destroyed" / "System under attack" alerts
- [x] **PRIORITY: Corvette variants via tech (game-designer R48-5, updated R49-2):** 3 corvette variants unlocked by T2 techs (same 100M + 50A build cost, 500-tick build time). Rock-paper-scissors combat triangle: **Interceptor** (T2 Physics: Advanced Reactors) — 8 HP, 5 attack, 30 ticks/hop. Fast and deadly. Beats Gunboats (attacks first via speed priority), loses to Sentinels. Maintenance: 1E. **Gunboat** (T2 Engineering: Deep Mining) — 15 HP, 4 attack, 50 ticks/hop. Tanky heavy hitters. Beats Sentinels (outlasts regen with raw DPS), loses to Interceptors. Maintenance: 2E + 1A. **Sentinel** (T2 Society: Gene Crops) — 12 HP, 3 attack + 2 HP regen/round, 40 ticks/hop. Sustain fighters. Beats Interceptors (heals through damage), loses to Gunboats. Maintenance: 1E + 2A. Build via `buildCorvette { variant: 'interceptor'|'gunboat'|'sentinel' }` — server validates T2 tech is completed. Track `ship.variant` on military ship object. Combat targeting: each type prioritizes its counter-target. Base corvette (10 HP, 3 attack, 40 ticks/hop) remains available as balanced option. All count toward MAX_CORVETTES=10 cap. Creates fleet composition metagame — your tech path determines your military options. Client: show variant buttons in corvette build UI when corresponding T2 tech is completed. Add corvette variant build, combat interaction (including counter-targeting), movement speed, regen, and tech-gating tests
- [x] **BALANCE TWEAK — Gunboat attack reduction (game-designer R51-balance):** Reduce Gunboat attack from 4 to 3 in CORVETTE_VARIANTS to tighten the rock-paper-scissors triangle. Currently Gunboat has the highest HP×ATK product (60 vs Interceptor 40 and Sentinel 36+regen), making it the default-best choice. At 3 ATK, Gunboat's HP×ATK drops to 45, closer to parity. Gunboat's strength should come from durability (15 HP), not damage. Update CORVETTE_VARIANTS in game-engine.js. Update corvette variant tests
- [ ] **Fleet intelligence — espionage action (game-designer R51-6):** Add `espionage` command: spend 25 influence to reveal a target player's fleet composition (ship types, counts, variant distribution, total HP/DPS) for 3000 ticks (5 minutes). 600-tick cooldown per target player. Track `_espionageReveals = Map<playerId, Map<targetId, expiryTick>>`. In `getPlayerState`, include target's fleet details if espionage is active. Show "Fleet Report" panel accessible from scoreboard showing revealed data. Also: ships in the same system as enemy ships (while at peace/neutral) can passively see enemy fleet composition in the system panel. Mutual-friendly players always see each other's fleet composition. Makes the corvette rock-paper-scissors triangle strategically meaningful — you can scout before committing. Add espionage tests
- [ ] **War weariness — occupation VP decay (game-designer R48-8):** After 500 ticks (50 seconds) of continuous occupation, the attacker's occupation VP decays by 1 per 200 ticks. At 900 ticks, occupation VP reaches 0. Defender's VP penalty also decays on the same schedule. Track `colony.occupationStartTick` (set when `occupiedBy` first becomes non-null, reset on liberation). In `_calcVPBreakdown`, compute effective occupation VP as `max(0, OCCUPATION_ATTACKER_VP - Math.floor(Math.max(0, ticksElapsed - 500) / 200))` for attacker, and symmetric decay for defender penalty. Creates natural "war seasons" — take, hold, profit briefly, then move on or liberate. Combined with 600-tick diplomatic cooldown, prevents degenerate permanent-occupation farming. No constant changes needed — just a decay formula in VP calculation. Add occupation VP decay tests (fresh occupation, at 500 ticks, at 900 ticks, after liberation)
- [x] **PRIORITY: NPC raider fleets — PvE military threat (game-designer R33, updated R39-1):** Every 1800-3000 ticks (3-5 min), spawn a raider fleet at a random edge/unknown system. Raiders move toward nearest player colony via BFS along hyperlanes at 40 ticks/hop (4 sec). Raider stats: 30 HP, 8 attack/tick. Players build "defense platforms" at colonies: `buildDefensePlatform` command, cost 100 alloys, 200 tick build time, max 1 per colony. Platform stats: 50 HP, 15 attack/tick. Combat auto-resolves over 5 ticks when raider enters defended system: platform deals 15/tick (75 total → kills 30 HP raider), raider deals 8/tick × 5 = 40 (platform survives at 10 HP). Second raider hitting before repair = colony raided. Platforms repair 10 HP/month passively. Undefended colony: 2 random districts disabled for 300 ticks + 50 of each resource stolen. +5 VP for destroying a raider (added to `_calcVPBreakdown`). Track `this._raiders = [{ id, systemId, path, hopProgress, hp }]` and `colony.defensePlatform = { hp, maxHp, building }`. Broadcast `raiderSpawned`, `raiderDefeated`, `colonyRaided` events. Render raiders as red diamond on galaxy map. Process in tick loop via `_processRaiderSpawning()` and `_processRaiderMovement()`. Creates alloy sink (100 alloys × 5 colonies = 500 alloys), defensive planning, and lays groundwork for PvP combat. Reference: Civ VI barbarians, Stellaris space creatures

### Phase 6: Diplomacy & Interaction
- [x] **PRIORITY: Diplomatic stances (game-designer R43-3, confirmed R45-2):** Add 3 diplomatic stances toward each other player: Neutral (default), Hostile (enables combat), Friendly (enables shared vision + trade bonus). Track `playerState.diplomacy = Map<targetPlayerId, { stance, cooldownTick }>`. `setDiplomacy` command: `{ targetPlayerId, stance }`, costs 25 influence, 600-tick cooldown between changes toward same player. **Neutral:** no combat, no trade bonus, ships pass freely — corvettes from neutral players can coexist in the same system without combat. **Hostile:** combat triggers when fleets meet (requires R43-1 fleet combat), can occupy colonies. Declaring hostile is mutual — auto-sets target's stance toward you to hostile. Broadcast "WAR DECLARED" event. **Friendly:** shared fog of war vision (each player sees the other's surveyed systems + systems within 2 hops of their owned systems), +10% production on colonies within 3 hops of friendly player's colony (apply as multiplier in `_calcProduction`, BFS distance check). Cannot attack. Broadcast "ALLIANCE FORMED". Friendly is mutual — requires acceptance via `acceptDiplomacy` command. VP: +5 VP at game end per friendly relationship, +10 VP if mutual. Scoreboard shows stance icons next to each player. Influence finally has a meaningful recurring sink. Add diplomatic stance tests (stance changes, cooldown, mutual hostile, combat gating, neutral coexistence, friendly shared vision, friendly bonus)
- [ ] **PRIORITY: In-game chat + diplomacy pings (game-designer R39-7):** Enable the existing chat message system during gameplay (infrastructure already exists for room chat). Client: show `#game-chat` panel during game phase, enable chat input. Server: route `chat` messages to all players in same game room (already handled). Add `diplomacyPing` command: `{ targetPlayerId, pingType: 'peace'|'warning'|'alliance'|'rival' }`. Broadcast ping to target player as event. Render ping as temporary colored icon on sender's territory on galaxy map (green=peace, yellow=warning, blue=alliance, red=rival). Icon fades after 300 ticks. No mechanical effect — pure communication. Minimum viable social layer that transforms parallel solitaire into a social experience. Add diplomacy ping tests
- [ ] **System claims with influence (game-designer R34):** Before colonizing, players can spend 25 influence to "claim" a system — prevents other players from colonizing planets there. Track `galaxy.systems[id].claimedBy = playerId`. Add `claimSystem` command: validate influence cost, system not already claimed/colonized by another player, system must be surveyed. Claims visible on galaxy map as colored borders (thin colored line around claimed system nodes). Auto-claim system when colonizing (free — included in colony ship cost). +1 VP per active claim in `_calcVPBreakdown`. First non-combat inter-player interaction. Add claim tests
- [ ] **Inter-player trade routes (game-designer R34):** Trade routes between two players' colonies generate +3 energy/month per hyperlane hop for both players. Add `proposeTradeRoute` command (sender specifies own colony + target player colony). Target player accepts via `acceptTradeRoute`. Track as `_tradeRoutes = [{ id, player1, colony1, player2, colony2, hops, accepted }]`. Max 3 trade routes per empire. Process income in `_processMonthlyResources`. Trade routes severable by either player via `cancelTradeRoute`. Creates economic interdependence and first cooperative multiplayer mechanic. Add trade route tests
- [ ] **Cease-fire negotiations (game-designer R48 content wishlist):** When two hostile players have been at war for 600+ ticks, either can propose a cease-fire via `proposeCeasefire { targetPlayerId }`. If accepted within 300 ticks (30 seconds), both players go to neutral stance (bypassing cooldown) and each gains +3 VP ("Peace Dividend"). Track `_ceasefireProposals = Map<key, { proposerId, targetId, tick }>` where key is sorted player ID pair. Accept via `acceptCeasefire { proposerId }`. Expire unaccepted proposals after 300 ticks. Cannot propose if cooldown hasn't elapsed since last ceasefire between same pair (use 1200-tick ceasefire cooldown). Emit `ceasefireProposed`, `ceasefireAccepted`, `ceasefireExpired` events. Creates a diplomatic offramp for wars and rewards de-escalation. Add ceasefire proposal, acceptance, expiry, VP, and cooldown tests
- [ ] **Non-Aggression Pact (game-designer R42):** Add `proposeNAP` command: costs 25 influence from each player. Lasts 3000 ticks (5 min). Prevents fleet attacks between signatories. Track as `_agreements = [{ type: 'nap', player1, player2, ticksRemaining }]`. Process in tick loop — decrement timer, remove on expiry. Broadcast `napFormed` / `napExpired` events. Display NAP icon on scoreboard next to allied players. First formal diplomatic agreement. Add NAP proposal, acceptance, and combat-prevention tests
- [ ] **Trade Agreement (game-designer R42):** Add `proposeTrade` command: costs 25 influence each. Mutual +15% energy and mineral income for both parties. Breaks on aggression (attacker loses 50 influence as betrayal penalty). Track as agreement with `type: 'trade'`. Applied as multiplier in `_processMonthlyResources`. Max 2 trade agreements per player. Show trade income bonus in resource panel. Add trade agreement tests
- [ ] **Sabotage action (game-designer R42):** Add `sabotageCrisis` command: spend 50 influence to trigger a random crisis on an opponent's colony. 600-tick cooldown per target. Cannot use on NAP/alliance partners. Broadcast "Foreign agents have caused [Crisis] on [Colony]!" to target. Track cooldown as `_sabotageCooldowns = Map<targetId, lastTick>`. First offensive diplomatic tool. Add sabotage tests
- [ ] Diplomatic stances: each player has a stance toward every other player — Neutral (default), Friendly, Rival, War. Changing stance costs influence (10 to change). War can only be declared from Rival stance (must be Rival for 60 seconds first). Peace can be proposed after 120 seconds of war
- [ ] Communication: players can send diplomatic messages to each other (text + optional trade offer). Messages appear in a diplomacy inbox panel
- [ ] Trade system: players can propose trades — exchange resources, systems, or treaties. Both players must accept. Active trades last until cancelled or war declared. Trade routes: +10% energy income between trading partners
- [ ] Alliances: Friendly players can form alliances (costs 50 influence each). Allied players share vision, cannot attack each other, and are pulled into each other's wars. Max 1 alliance per player (with 1 other player in 2-player, or faction-based in larger games)
- [ ] War & conquest: when at war, fleets can attack enemy systems. Conquering a colony (destroy starbase + have fleet in system for 30 seconds) transfers it to the attacker. Conquered pops are unhappy (-50% output for 120 seconds). War exhaustion builds over time — after 300 seconds, forced peace option becomes available
- [ ] Diplomacy UI: player list with stances, diplomacy inbox, trade proposal panel, alliance status, war/peace controls
- [x] **Underdog bonus and catch-up mechanics (game-designer R47-5):** Players controlling fewer colonies than the leader get +15% resource production per colony gap (max +45%, i.e., 3 colony gap), applied as multiplier in `_processMonthlyResources`. Calculate each month: find max colony count across all players, compute deficit for each player, apply `1 + (deficit * 0.15)` multiplier (capped at 1.45) to all positive production. Show "Underdog Bonus: +X%" indicator in resource panel when active. Only active in 2+ player games. Each tech tier costs 15% less per player who has already completed it. Prevents snowballing from occupation death spiral while keeping the leader's advantage meaningful. Add underdog bonus production tests
- [ ] Match timer and victory points: optional hard timer selectable in room settings (30/45/60 minutes, or unlimited). At expiry, highest VP wins. VP = colonies×10 + techs×5 + fleets×2 + allies×15. Show VP scoreboard accessible via hotkey (Tab)
- [ ] Surrender vote: in 3+ player games, 2+ players can vote to concede, ending the match with the current VP leader winning. Prevents hostage situations where eliminated players must wait
- [ ] **Colony mood system — tall vs wide tension (game-designer R51-7):** Add empire mood based on colony count that creates diminishing returns on expansion. **Thriving (1-2 colonies):** +10% all production. **Content (3 colonies):** no modifier. **Restless (4 colonies):** -5% all production, -25% pop growth. **Rebellious (5 colonies):** -10% all production, -50% pop growth. Applied as multiplier in `_calcProduction` based on `playerColonies.get(playerId).length`. Expansionist doctrine shifts thresholds up by 1 (Thriving at 1-3, Content at 4, Restless at 5). Track mood tier in `playerState.empireMood`. Show mood indicator in HUD and colony panel. Creates the tall-vs-wide strategic tension that defines great 4X games — going wide is powerful but costly. Reference: Civ VI amenities, Stellaris admin capacity. Add colony mood tests
- [ ] **Colony economic blockade (game-designer R30):** When a rival has a colony within 2 hyperlane hops of your owned system, you can deploy a "blockade marker" by spending 50 alloys. Besieged colony produces -25% on all resources until blockade is removed. Blockade costs the deployer 10 alloys/month to maintain. Blockade auto-removed if deployer runs out of alloys or loses the staging system. Only 1 blockade active per player. Track `colony.blockade = { byPlayerId, stagingSystemId }`. Add `deployBlockade` and `removeBlockade` commands. Broadcast blockade events. Creates multiplayer economic friction without combat. No fleet mechanics required — economic warfare only. Requires: owned systems/territory tracking
- [ ] **Galactic Council mid-game vote (game-designer R29):** At 50% match time elapsed, a "Galactic Council" forms. Every 3 in-game minutes (1800 ticks), a resolution is proposed to all players with 60-second voting window. Resolution pool (pick randomly, no repeats): "Mutual Research Pact" (+10% research output for all players for 3 minutes), "Economic Stimulus" (+10% mineral output for all for 3 minutes), "Demilitarization Treaty" (ship build times +50% for all for 3 minutes — prep for when ships exist), "Open Borders" (fog of war visibility +1 hop for all for 3 minutes). Each player casts yes/no vote. Resolution passes if majority votes yes. Track `this._councilActive`, `this._nextCouncilTick`, `this._activeResolution`. Add `voteCouncil` command (yes/no). Broadcast `councilResolution` event with proposal text and result. Show vote UI as centered modal overlay with 60-second countdown. Creates diplomatic interaction without formal diplomacy system. Only activates in 2+ player games

### Phase 7: Events, Polish & Win Conditions
- [x] **PRIORITY: Endgame crisis event (game-designer R34, updated R39-4, R43-5):** At 75% match timer elapsed, trigger a galaxy-wide crisis. Two variants (randomly selected): **Galactic Storm** — all production reduced by 25% for remainder of match (applied as 0.75 multiplier in `_calcProduction`, tracked as `this._galacticStorm = true`). Forces economic adaptation — players who stockpiled shine, barely-breaking-even players get punished. **Precursor Awakening** — hostile fleet spawns at galaxy center or random far system (60 HP, 15 attack, 30 ticks/hop toward nearest colony). +15 VP for destroying it. If it reaches undefended colony: occupies it (production halved, -5 VP). Multiple defense platforms can engage (reuses R39-1 combat resolution). 100-tick advance warning broadcast before crisis triggers. Emit `endgameCrisis` event with type and details. Show dramatic UI alert. Only activates with match timer enabled. Requires R39-1 raider infrastructure for Precursor variant. Add endgame crisis tests
- [ ] **PRIORITY: Mid-game catalyst events (game-designer R51-1):** Add 3 timed galaxy-wide events that fire at fixed match-time percentages to break the mid-game optimization valley. Only in timed matches. **Resource Rush (30% match time):** A random unsurveyed system is revealed to all players as containing a "motherlode" — first player to station a military ship OR colonize there gets +100 of a random resource per month for 1800 ticks (3 minutes). Emit `resourceRush` event with system ID and resource type. Track `this._resourceRushSystem` and `this._resourceRushOwner`. **Tech Breakthrough Auction (45% match time):** All players simultaneously bid influence (sealed bid, 60-second window) on instantly completing their current T2 research. Highest bidder gets it, all bidders lose their bid. Emit `techAuction` event with 60-tick response window. Track bids in `this._auctionBids = Map<playerId, amount>`. Resolve after window closes. **Border Incident (55% match time):** Two random players with colonies within 3 hops get a prisoner's dilemma — each independently chooses "escalate" or "de-escalate" within 60 ticks. Both de-escalate: +5 VP each. One escalates: escalator gets +3 VP, other forced to hostile stance. Both escalate: both go hostile, no VP. Emit `borderIncident` event. Add `respondIncident { choice }` command. Each event creates a decision with no obviously correct answer, forcing active engagement during the mid-game. Add mid-game catalyst tests
- [ ] Anomaly events: when surveying, anomalies trigger event chains. 10 unique events with choices: e.g., "Ancient Ruins" (choose: excavate for +500 research or preserve for +50 influence), "Derelict Ship" (salvage for free corvette or study for +200 engineering research), "Alien Artifacts" (+research or +alloys). Events add narrative flavor and meaningful choices
- [ ] Random galaxy events: every 120 seconds, chance of galaxy-wide event. Asteroid storm (random system loses 1 mining district), Solar flare (random system loses power for 30 seconds), Resource boom (random unclaimed system gets bonus resources). Notification to all players
- [ ] Planet biome rendering: distinct Three.js materials/textures per planet type. Continental (green/blue), Ocean (deep blue), Arctic (white/pale blue), Desert (tan/orange), Tropical (deep green), Arid (brown), Barren (gray). Visible in colony view as ground material and in system view as planet color
- [ ] Visual effects: fleet warp-in/warp-out animations, combat laser/explosion particles, building construction scaffolding animation, research complete flash, colony founding ceremony effect. Use Three.js particle systems and shader materials
- [ ] Sound design: ambient space music, UI click sounds, combat sounds, notification chimes, warp drive sound for fleet movement. Use Web Audio API
- [ ] **Distinct victory conditions (game-designer R42):** Add 3 instant-win conditions checked every monthly tick alongside existing VP timer win. **Scientific Victory**: complete all 3 T3 techs + survey 80% of galaxy systems → instant win. **Military Victory**: occupy 3 enemy colony systems simultaneously (requires fleet combat). **Economic Victory**: stockpile 1000 alloys + 500 influence + have 5 active colony traits → instant win. Check in `_processMonthlyResources` — if any condition met, call `_triggerGameOver` with victory type. VP remains as tiebreaker if timer expires before any condition is met. Show victory progress on scoreboard (3 progress bars). Add victory condition tests for each type
- [ ] Win conditions (selectable in room settings): Domination (control 60% of colonizable planets), Research Victory (complete all tier 5 techs in all 3 tracks), Economic Victory (accumulate 10000 of each basic resource simultaneously), Diplomatic Victory (be allied with or have friendly status with all surviving players for 120 continuous seconds). Default: Domination
- [ ] Galactic radio ticker: a shared event ticker at the top of the screen narrating galaxy events in-character ("BREAKING: Player X reports mineral boom on Kepler-7b", "ALERT: Fleet detected near Orion Nebula", "TRADE: Player Y offers 500 minerals"). Events include colonizations, anomaly discoveries, wars declared, tech breakthroughs. Creates shared narrative layer for multiplayer
- [ ] Post-game screen: on `gameOver`, show overlay with winner, victory type, per-player stats (colonies, techs researched, ships built, ships lost, resources gathered, pops), timeline graph of territory over time. "Return to Lobby" button
- [ ] **Post-game VP timeline graph (game-designer R30):** Record VP snapshots for all players every 10 months (1000 ticks) during gameplay in `this._vpTimeline = []` — each entry is `{ tick, players: { [id]: vp } }`. Include in gameOver message. Client renders a simple line chart (Canvas 2D or HTML table with bar widths) showing each player's VP over time, colored by player color. Shows inflection points, surges, and comebacks. Low effort, high emotional payoff — players learn from their pacing. Place above the per-player score breakdown on the post-game screen
- [ ] Player disconnect handling: on disconnect, start 60-second grace period. AI takes over basic colony management (auto-queue food if deficit, continue existing research). If player reconnects, restore session. If timeout, colonies continue on auto-pilot as "fallen empire" (passive, doesn't expand)
- [ ] ~~**Scarcity seasons:**~~ *(Moved to Phase 2 — see "Scarcity seasons" entry there)*
- [ ] **Dynamic galactic news ticker (game-designer R28):** Shared scrolling text feed at top of game screen narrating events in-character with procedural flavor text templates. Takes existing gameEvent types and wraps them: `colonyFounded` → "BREAKING: [Player] establishes [Colony Name] in the [System] system", `constructionComplete` → "INDUSTRY: [Colony Name] completes new [District] district", `researchComplete` → "SCIENCE: [Player] achieves breakthrough in [Tech Name]", `popMilestone` → "CENSUS: [Colony Name] reports population reaching [N]". Template pool of 3-4 variants per event type for variety. Display as single-line ticker, top-center, cycling through messages at 4-second intervals. Max 8 queued messages, oldest discarded. Player name colored by player color. System messages in neutral gray. Creates shared narrative layer — makes the galaxy feel alive and other players feel present. Client-only feature using existing event data
- [x] **PRIORITY: Colony procedural naming (game-designer R43-7):** When founding a colony, generate a name based on planet type from curated word lists — Arctic: "New Helsinki" / "Frostheim" / "Boreas Station", Desert: "Dusthaven" / "Sunward" / "Dune Prime", Ocean: "Tidecrest" / "Pelagius" / "Deepreach", etc. 8-10 names per planet type. Players can rename colonies but procedural names add personality and memorability
- [ ] **Spectator replay time-lapse:** After a match ends (gameOver), record colony snapshots (district count, pops, resources) every 10 months during gameplay in a `this._replaySnapshots[]` array in game-engine. On game over, include replay data in the gameOver message. Client renders a 30-second time-lapse animation: fast-forward through the colony grid, districts appearing, pop counter climbing, resources flowing. Use Three.js animation with setInterval stepping through snapshots at 100ms each. Skip button to go straight to results. "One more game" emotional hook — players see their colony's story compressed into half a minute
- [ ] **Colony rivalry ticker (multiplayer):** In multiplayer games, broadcast comparison events when one player surpasses another: "Player B's colony just surpassed yours in population", "You have the highest Research output in the galaxy", "Player A built their 10th district — you have 7". Compare pops, district count, and net resource income across players every 2 months (200 ticks). Send as `rivalryEvent` type through existing gameEvent system. Only send to the players involved (not broadcast). Creates competitive tension through social comparison without direct conflict. Suppress in practice mode
- [ ] ~~**Speed Chess mode (DEPRIORITIZED — game-designer R15):** Timer-based modes are deprioritized until the galaxy/fleet loop exists. Can revisit as a "Blitz" option after domination victory works~~
- [ ] ~~**Surface anomalies on colony grid:**~~ *(Moved to Phase 1 — see "PRIORITY: Surface anomalies on colony grid" entry there)* When creating a colony, randomly place 1-3 tile anomalies on the district grid. Types: "Ancient Ruins" (excavate for +500 research, or preserve for +2 influence/month), "Mineral Vein" (+50% output to mining district built on this tile), "Thermal Vent" (+50% output to generator district built on this tile), "Fertile Soil" (+50% output to agriculture district on this tile), "Alien Artifact" (one-time choice: +200 alloys or +300 research). Track as `colony.anomalies = [{ tileIndex, type, resolved }]`. Anomalies make each colony spatially unique and create a puzzle in district placement. Add `resolveAnomaly` command handler. Render anomaly markers on colony grid tiles
- [ ] ~~**Pressure Cooker mode (REMOVED — game-designer R15):** Removed from roadmap. Shared-planet mode is a gimmick that distracts from the core 4X loop of planets and ships. Can be revisited as a novelty party mode after the galaxy is built~~
- [ ] **Dynamic colony atmosphere (stretch, game-designer R14):** As colony develops, add environmental effects to Three.js scene. At 25% capacity: subtle particle system (dust for Desert, snow for Arctic, rain for Ocean, spores for Tropical). At 50%: faint city glow on horizon (PointLight, warm color). At 100%: skybox shifts from dark space (#0a0a1a) to planet-appropriate atmosphere color. Track development ratio in renderer, trigger effects at thresholds. Makes colony progression visible at the environmental level
- [ ] **Tech tree branching dilemma (stretch, game-designer R14):** At T2, offer two mutually exclusive tech options per track. Physics T2: Advanced Reactors (+50% Generator) OR Laser Arrays (+30% Generator, stores `combatBonus: 1.1` for future fleet damage). Society T2: Gene Crops (+50% Agriculture) OR Colonial Ambition (+25% Agriculture, +50 influence). Engineering T2: Deep Mining (+50% Mining) OR Alloy Refinement (+25% Mining, +50% Industrial). Add `exclusive` field to TECH_TREE entries. `setResearch` rejects if the exclusive alternative is already completed. Forces permanent specialization within tracks
- [ ] **Galactic Wonder Race (stretch, game-designer R20):** 3 galactic wonders that can only be built once galaxy-wide. First player to complete one claims it permanently. "Dyson Sphere" (cost: 1000 alloys, 500 energy; effect: +50 energy/month to all colonies), "Galactic Library" (cost: 1000 total research across all types; effect: all T1 techs researched for free), "Ring World" (cost: 500 alloys + 500 minerals; effect: new colony with size 25 planet). Track `galaxy.wonders = { dysonsphere: { claimedBy, progress }, ... }` in game-engine. Add `buildWonder` command — requires alloys/minerals/research stockpile. Progress tracked per month (spend X resources per month until complete). Broadcast `wonderStarted` (all players see who's building what) and `wonderCompleted` events. Show wonder progress in galaxy view as a special system icon. Creates race dynamics and end-game VP spikes (each wonder worth +20 VP). Inspired by Civilization's world wonders
- [ ] **Secret rival objectives (stretch, game-designer R27):** At game start in multiplayer, assign each player a hidden objective targeting another player (round-robin so everyone targets someone different). Pool of objectives: "Control more colonies than [Player]" (+10 VP), "Have higher total pops than [Player]" (+10 VP), "Have a colony within 2 hops of [Player]'s capital" (+8 VP), "Out-research [Player] (higher total research)" (+10 VP), "Build more districts than [Player]" (+8 VP). Store as `playerState.secretObjective = { type, targetPlayerId, description, vpReward }`. Evaluate at game end in `_triggerGameOver`. Show in post-game scores as "Secret Objective: [description] — [Completed/Failed]". Players don't know who is targeting them, creating invisible competition. Only assign in 2+ player games. Add objective evaluation tests
- [ ] **Faction archetypes at game start (stretch, game-designer R20):** At game start, each player picks from 4 faction archetypes that modify starting conditions: "Miners' Guild" (start with 2 Mining districts instead of 1, +25% mineral output), "Science Collective" (start with 1 Research district, first T1 tech starts 50% complete), "Colonial Authority" (start with 10 pops instead of 8, free colony ship token for first colonization), "Energy Cartel" (start with 2 Generators, +50% energy output). Server sends `factionSelect` screen with 4 options. 30-second timer, random if no choice. Faction visible to all players on scoreboard. Adds asymmetric gameplay and metagame — which faction counters which? Inspired by Endless Space 2 factions but lighter. Add faction selection tests

## Conventions

- Server modules: plain `module.exports`, no dual-export
- Client modules: IIFE with `window.*` for browser, `module.exports` for Node.js tests
- Tests: `node:test` + `node:assert` (Node.js built-in)
- All game state is server-authoritative
- Commands: client sends intent, server validates and executes
- State broadcast: server sends colony/fleet updates each tick (optimize later with deltas)
- Three.js for all rendering — no 2D canvas, no sprites
- Isometric colony view: OrthographicCamera at 35.264° pitch, 45° yaw
